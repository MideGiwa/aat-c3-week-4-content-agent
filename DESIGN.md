# AI Content Research & Publishing Agent — Design Document

**Project:** Week 4 — AI Content Research and Publishing Agent (Koya Talent)
**Status:** Draft for review — decisions below reflect direction chosen on 2026-09-14
**Author:** Mide Giwa

---

## 1. Business Problem (Simplified)

Koya Talent's content team already has a working pipeline: brainstorm an idea, research it, write an SEO article, adapt it for LinkedIn/X/newsletter, review internally, then publish. The problem isn't quality — it's that every step is manual, so the team can't scale volume without either hiring more people or letting quality, tone, and factual accuracy slip.

The goal is an AI agent that takes over the repetitive middle of that pipeline (research, source selection, drafting, self-checking, channel adaptation) while a human still makes the final call before anything goes out. Two things can't be compromised: the content must stay traceable to real sources, and nothing publishes without human approval.

## 2. Proposed Solution (Overview)

A staged content pipeline, triggered by a content request (idea or URL + audience), that runs research and drafting largely autonomously, self-evaluates its own output against a fixed rubric, revises weak drafts automatically, and then hands off to a human reviewer. Once approved, the system prepares LinkedIn/X/newsletter versions and drops everything into a publishing queue — it does not push directly to the live platforms in this build.

Every step is logged (inputs, outputs, sources used, evaluation scores) so a failure at any stage is easy to trace, and so the final output can show exactly which sources informed it.

### Decisions locked in for this build

- **Orchestration:** Hybrid — n8n handles triggers, stage sequencing, the review/approval interface, and the publishing queue; a small custom service (or n8n Code nodes) handles the heavier LLM calls (planning, drafting, structured-output evaluation) where more control over prompts/parsing is useful.
- **Publishing scope:** Queue-only for this build. Approved content lands in a Supabase table with status `ready_to_publish`, tagged by channel — no live LinkedIn/X/email API integration yet.
- **Retrieval:** Supabase + pgvector. Source excerpts are embedded and stored so sections can be matched to the excerpts that actually support them, and citations stay attached at the excerpt level rather than just "here are 5 links we looked at."

## 3. System Architecture

**Revised 2026-09-16 — see the Decisions Log entry below.** The pipeline (research through channel prep) moved from "n8n orchestrating a standalone Python LLM service" into the same Next.js app as the front-end, running as server-side code. n8n's role is now limited to what it was always uniquely good at here — the scheduled publish runner — not the whole backbone.

```
┌──────────────────────────────────────────────────────────────────────┐
│  Custom Front-End + Pipeline (Next.js, one app)                       │
│                                                                        │
│  Intake ──▶ Research/Retrieval ──▶ Source Curation ──▶ Planning       │
│  (validate,   (Firecrawl scrape      (rank/select        (propose     │
│   never trust  or Tavily/Exa search,  by relevance,        2+ angles, │
│   client input) Supabase + pgvector)  reject the rest)     pick one)  │
│                                                               │       │
│                                                               ▼       │
│                                                    Draft Generation   │
│                                                    (Claude API)       │
│                                                               │       │
│                                                               ▼       │
│                                                    Self-Evaluation    │
│                                                    (rubric, JSON,     │
│                                                     "never fail open")│
│                                                               │       │
│                              revise ◀──── fail ──────┤ pass ──────┐   │
│                          (targeted fix,                              │
│                           re-evaluate,                                │
│                           capped rounds)                              │
│                                                                    ▼  │
│                                                Channel Prep (LinkedIn,│
│                                                X, Newsletter — all    │
│                                                three generated here,  │
│                                                status "draft")        │
│                                                                    │  │
│                                                                    ▼  │
│  Human Review (approve / reject / request changes) ◀─────────────────┘
│      on approval: this request's channel assets → "ready_to_publish"  │
└────────────────────────────────────┬──────────────────────────────────┘
                                      ▼
                        ┌───────────────────────┐
                        │  Publishing Queue       │  Supabase table,
                        │  (Supabase table)       │  status ready_to_
                        └───────────────────────┘  publish, per channel.
                                                    This *is* the "publish
                                                    or schedule for release"
                                                    step (2026-09-18) —
                                                    nothing polls or auto-
                                                    publishes it; see §11.

Logging: every stage writes to the request's activity log (`activity_log`
table) as it runs, so a failure anywhere is traceable end to end — this
replaces the earlier per-service `runs`/`stage_logs` table concept now that
there's one process instead of several handing off over HTTP.
```

### Component breakdown

- **Custom Front-End + Pipeline (Next.js + Supabase):** one app, doing everything from intake through channel prep, plus serving as the Human Review UI. The browser never talks to anything but this app's own server routes; those routes call the pipeline functions directly and write to Supabase with the service role key — no separate service, no webhook shared secret to manage. See `FRONTEND-SPEC.md` for the UI spec and `CONTENT-PIPELINE-SPEC.md` for the pipeline itself (code structure, request field table, and the reasoning behind this revision).
- **Intake:** collects idea/topic, target audience, optional source URL, optional supporting material as text or an uploaded file (`.txt`, `.pdf`, spreadsheet — `.xlsx`/`.csv`, for numbers/data-heavy input — or `.docx`; anything else is rejected at upload), desired tone, priority channels, and either "publish as soon as approved" or a specific future schedule (past dates/times are rejected both in the front-end's UI and again server-side on receipt — never trusted from the client alone). Attachments can be edited or removed any time before the request has actually started research (see §14).
- **Research/Retrieval:** if a URL is given, Firecrawl scrapes it directly; if it's a raw idea, a search API (e.g. Tavily/Exa, or Firecrawl's search) finds 3–5 candidate reference sources, which are then scraped and chunked.
- **Source Curation:** ranks and selects the chunks/sources that actually matter for this piece and rejects the rest — tracked with a relevance note either way, so "why was this excluded" is always answerable (visible in the review UI's Sources panel, including the rejected ones, collapsed by default).
- **Supabase + pgvector:** chunks are embedded (Voyage AI) and stored with metadata (source URL, title, retrieved_at). Retrieval at drafting time is a similarity search against the request's topic/outline, not just "dump all sources in."
- **Planning → Draft Generation → Self-Evaluation → Revision (Claude API, called directly from Next.js server code):** proposes 2+ angles and picks one; drafts structured sections from the plan + curated sources; scores the draft against the rubric (default 9 criteria plus any custom ones, DESIGN.md §13) with a schema-validated JSON response — a schema failure is a hard error, never silently treated as a pass; and, if the evaluation flags specific sections, revises only those (capped rounds) rather than regenerating from scratch. Full contract in `CONTENT-PIPELINE-SPEC.md`.
- **Channel Prep:** once the draft clears evaluation (or hits the revision cap), generates LinkedIn, X, *and* newsletter versions per each channel's formatting rules — all three, every time a channel is in the request's priority list, not gated on which channels currently have automated publishing. Newsletter *content* has always been in scope; only its *distribution* is still undecided (Decisions Log, §10), and that's a separate concern from whether the copy gets generated and is reviewable.
- **Human Review (the same front-end):** shows every draft version with its full evaluation/revision/activity timeline, the source list, and the per-channel copy; supports **approve** (promotes this request's channel assets to `ready_to_publish`), **reject** (terminal — kills the request), or **request changes** (triggers an immediate targeted revision pass with the reviewer's own notes, then returns to pending review with the new version — `CONTENT-PIPELINE-SPEC.md` §7). More than one reviewer can be assigned to a request; "first decisive action wins" is enforced with a database unique constraint (`review_decisions`, §6); a decision aimed at a draft the pipeline has since superseded is also rejected (`stale_draft`), not silently applied to stale content — see `FRONTEND-SPEC.md` §"Flow 2."
- **Publishing Queue:** a Supabase table — satisfies the PRD's "saved into a clear publishing queue"/"publish or schedule for release" bar on its own (2026-09-18, §11). Every priority channel's copy lands here once approved, including newsletter, with `scheduled_for` capturing the schedule chosen at approval. Nothing currently polls or auto-publishes these rows to LinkedIn/X — that's a deliberate scope decision, not a gap (§11).

## 4. Tool Stack

| Layer | Tool | Why |
| --- | --- | --- |
| Front-end + Pipeline | Next.js + Supabase (Auth, Storage, Realtime) | One app for intake, the full research→channel-prep pipeline, and review; real client-side validation (a date picker that can't select the past, instant file-type checks) plus a trusted server layer that writes to Supabase directly — no separate service to call over the network — see `FRONTEND-SPEC.md` and `CONTENT-PIPELINE-SPEC.md` |
| Publishing orchestration | None — Supabase `publishing_queue` table only | n8n is fully retired (2026-09-18); the queue itself, populated on approval, is the "publish or schedule for release" deliverable. Nothing polls or auto-publishes it. See §11 and the 2026-09-18 Decisions Log entry |
| LLM generation & evaluation | Claude API (structured outputs), called directly from Next.js server code | Planning, drafting, rubric-based self-evaluation, revision — structured outputs keep evaluation scores and source citations machine-readable; `CONTENT-PIPELINE-SPEC.md` |
| Scraping | Firecrawl | URL-based extraction for the URL-based request scenario |
| Search | Tavily / Exa (or Firecrawl search) | Finds candidate sources for raw-idea requests with no URL |
| Embeddings | Voyage AI (`voyage-3-lite`/`voyage-3`) | Anthropic doesn't offer a first-party embeddings endpoint; Voyage is Anthropic's recommended embedding partner and pairs cleanly with pgvector |
| Retrieval store | Supabase (Postgres + pgvector) | Embedding storage, similarity search, and the system of record for requests, sources, drafts, evaluations, and the publishing queue |
| Notifications | Discord (incoming webhook) | Alerts a human when something needs attention — failures, stale posts, repeated failures — without needing a bot/OAuth setup |

## 5. Pipeline Stages (detailed)

1. **Intake** — request captured with idea/topic, audience, optional source URL, tone, priority.
2. **Research & Retrieval** — scrape (if URL) or search + scrape (if raw idea); chunk and embed into Supabase.
3. **Source Curation** — LLM ranks/selects the excerpts worth using, discards irrelevant or low-quality material.
4. **Content Planning** — outline with primary/secondary keywords, section structure, and a source-to-section map.
5. **Draft Generation** — 2–3 article options generated per the SEO best practices, each grounded in mapped sources with inline citations. The full source list is always tracked internally (traceability is non-negotiable per the PRD), and — where appropriate per the SEO best practices' link guidance — relevant sources are also surfaced in the article itself, either as inline links at the claims they support or as a closing "Sources"/"Further reading" section, not only kept in an internal-only audit trail.
6. **Self-Evaluation** — Claude scores the draft against the rubric (topic relevance, source grounding, factual consistency, audience fit, tone, SEO fit, channel fit, clarity, completeness) and returns pass/revise/reject plus specific notes.
7. **Revision Loop** — "revise" triggers targeted regeneration of only the flagged sections, then re-evaluation; capped at 2–3 rounds, with every round's scores and notes preserved as history.
8. **Human Review** — reviewer sees the article(s), full evaluation/revision history, and sources; approves, rejects, requests a revision, or selects among options.
9. **Channel Adaptation** — approved article is adapted into LinkedIn, X, and newsletter formats per the formatting rules.
10. **Publishing Queue** — each channel asset is written to the queue table with status `ready_to_publish`, still linked to its source list.
11. **Logging** — every stage writes structured logs (inputs, outputs, errors) keyed by request ID for debugging.

## 6. Data Model (Supabase — initial sketch)

- `profiles` — id (matches Supabase `auth.users.id`), name, email, roles (array: `content_manager`/`reviewer`/`admin`), created_at
- `requests` — id, idea/topic, audience, source_url, tone, status, submitted_by (→ `profiles`), created_at
- `request_reviewers` — request_id, reviewer_id (→ `profiles`) — who's assigned to review a request; replaces an earlier informal "reviewers" field
- `review_decisions` — id, draft_id (or channel_asset_id), action (`approve`/`reject`/`request_changes`), reviewer_id, notes, decided_at — **UNIQUE constraint on the target (draft_id/channel_asset_id)**, so two reviewers can never both successfully record a decision on the same thing; this is what actually enforces "first decisive action wins" (§7), at the database level rather than as an app-level check that itself could race
- `request_attachments` — id, request_id, file_type (`txt`/`pdf`/`spreadsheet`/`docx`), file_url, status (`uploaded`/`processing`/`corrupt`/`removed`), uploaded_at, removed_at
- `sources` — id, request_id, url, title, retrieved_at, raw_text
- `source_chunks` — id, source_id, chunk_text, embedding (pgvector), relevance_notes
- `drafts` — id, request_id, version (int, always incrementing — a regeneration never overwrites, it creates the next version), content, option_label, generated_by (`system_initial`/`system_revision`/`human_requested`), created_at
- `evaluations` — id, draft_id, round, status (pass/revise/reject), scores (jsonb), notes, created_at
- `rubric_criteria` — id, name, description, scope (`default`/`custom`), applies_to_channel (nullable — null means "all channels"), applies_to_content_type (nullable), weight, created_by, active (bool)
- `channel_assets` — id, request_id, draft_id, channel (linkedin/x/newsletter), content, status
- `publishing_queue` — id, channel_asset_id, status (`ready_to_publish` / `processing` / `published` / `failed` / `stale_needs_review`), scheduled_for, published_at, error_message
- `activity_log` — id, request_id, actor_type (`system`/`human`), actor_id, action (e.g. `regenerate_draft`, `approve`, `reject`, `request_changes`, `add_reviewer`, `remove_attachment`), target (e.g. draft id, attachment id), notes, created_at — this is the human-facing timeline; `stage_logs` stays the machine-facing debug log
- `stage_logs` — id, request_id, stage, status, error_message, created_at

## 7. Human Review & Approval

Nothing reaches `publishing_queue` without an explicit approval action recorded against a specific draft/channel-asset.

**Reject vs. Request Changes are two distinct actions, not one "reject" button:**
- **Reject** is terminal. The request ends (`status = rejected_by_human`), the submitter is notified, and nothing further happens automatically — no auto-regeneration. If the idea is worth revisiting, that's a new request.
- **Request Changes** sends the current draft back into the revision loop with the reviewer's notes attached, the same mechanism the automatic rubric-triggered revision uses (see §5, step 7) — just triggered by a human instead of a failing rubric score. It's still capped at the same 2–3 rounds and still preserves every round's history.

**Multiple reviewers:** a request can have more than one assigned reviewer (`request_reviewers`, §6). Default collaboration model — **first decisive action wins**: whichever reviewer approves, rejects, or requests changes first is the recorded decision, enforced by the `review_decisions` table's unique constraint on the target draft/channel-asset (§6) — a database-level guarantee, not just an app-level check, so a second conflicting action can't slip through a race window. It's refused with "already decided by \<reviewer\>" via a `409` response (`FRONTEND-SPEC.md` §"Flow 2"), and the front-end's Supabase Realtime subscription surfaces this live to anyone else looking at the same draft. Any reviewer can leave comments at any time regardless of who has final say, and every action from every reviewer is recorded in `activity_log` for full accountability. **Whether some content should require sign-off from more than one reviewer (e.g. anything client-facing or compliance-sensitive) instead of "first wins" is a genuine policy question** — see `BUSINESS-QUESTIONS.md`.

**Version history, regeneration & activity trail (draft stage, step 4):** yes — a draft can be regenerated at any point, whether triggered automatically (failed rubric check) or manually (a reviewer's "request changes" or an explicit "regenerate" action). Every regeneration writes a *new* row to `drafts` with an incremented `version` — nothing is overwritten in place — so the full version history is always available to page through. Two views are available side by side: the **evaluation timeline** (every round's rubric scores and notes, from `evaluations`) and the **activity trail** (every human and system action — who did what, when — from `activity_log`). Together they answer "how did we get from v1 to the version that got approved."

## 8. Mapping to PRD Test Scenarios

| PRD Scenario | Covered by |
| --- | --- |
| Raw idea request | Search + scrape branch in Research & Retrieval |
| URL-based request | Firecrawl branch in Research & Retrieval |
| Source grounding | pgvector-backed source-to-section mapping, inline citations |
| Evaluation & revision loop | Self-Evaluation + Revision Loop stages, `evaluations` table history |
| Human approval | Human Review stage, gated `publishing_queue` writes |
| Channel formatting | Channel Adaptation stage against formatting rules |
| Publishing/scheduling | `publishing_queue` table (queue-only scope), immediate-or-scheduled with past-date rejection at input |
| Failure handling | `stage_logs` table + n8n execution history, per-request isolation (§15) |
| Corrupt/invalid supporting material | `request_attachments.status = corrupt`, rejected at upload for disallowed file types |
| Draft regeneration & history | `drafts.version` (never overwritten), `evaluations` timeline, `activity_log` trail |
| Multi-reviewer conflicts | "First decisive action wins" constraint on draft/channel-asset decisions |

## 9. Open Questions for Stakeholders

These are the kinds of questions worth confirming with whoever assigned this project (a content lead, program manager, or client) before locking scope further:

- **Volume & cadence** — how many pieces of content per week/month should this handle? Affects whether a simple queue is enough or whether priority/concurrency handling matters.
- **Source credibility bar** — is any public source acceptable, or should research be restricted to a vetted domain list? How should conflicting sources be handled?
- **Number of draft options** — how many article options should the system generate, and should the system pre-select the best one per the rubric before a human ever sees it, or always show all options?
- **Approval workflow** — single reviewer, or a multi-step chain (e.g., content lead then account manager)? Does a rejection require a brand-new request or can it restart the pipeline from planning?
- **Brand voice specifics** — beyond the channel formatting rules doc, is there a fuller style/voice guide, especially if different clients or verticals need different tones?
- **Confidentiality** — any constraint on sending client source material to external APIs (Claude, Firecrawl, search providers)?
- **Cost ceiling** — is there a target cost per article, given multiple draft generations plus evaluation plus revision rounds?
- **Existing systems** — is there a CMS, social scheduler, or email platform this should eventually integrate with, or is the queue the end of scope for now?
- **Success metric** — how will "this worked" be measured after launch (time saved, volume increase, first-pass approval rate, etc.)?

## 10. Decisions Log

| Date | Decision | Chosen |
| --- | --- | --- |
| 2026-09-14 | Orchestration approach | Hybrid (n8n + custom LLM service) |
| 2026-09-14 | Publishing scope | Queue-only (no live platform APIs) |
| 2026-09-14 | Retrieval approach | Supabase + pgvector |
| 2026-09-14 | Publishing scope (revised) | Extended beyond queue-only — added an n8n workflow that polls the queue and publishes automatically |
| 2026-09-14 | Automated publish channels | LinkedIn and X only for now (Medium dropped, newsletter deferred/manual) |
| 2026-09-14 | Publish check interval | Every 5 minutes |
| 2026-09-15 | Newsletter scope | Newsletter *content generation* is in scope (channel adaptation produces it); the distribution mechanism (ESP, ConvertKit, plain email, etc.) is explicitly deferred to technical tool selection |
| 2026-09-15 | Scheduling | Requests can be published immediately or scheduled for a future date/time; past dates/times are rejected at input |
| 2026-09-15 | Supporting material formats | Uploaded attachments limited to `.txt`, `.pdf`, spreadsheet (`.xlsx`/`.csv`), `.docx` — other formats rejected at upload |
| 2026-09-15 | Multi-reviewer default | "First decisive action wins" is the default collaboration model; whether some content needs multi-reviewer consensus is an open business question |
| 2026-09-15 | Rubric extensibility | Rubric criteria beyond the PRD's default 9 can be added, scoped per channel/content type, without modifying the default rubric |
| 2026-09-16 | Notifications | Added a Discord webhook notification service for events needing human attention; scoped as an alert channel (failures/escalations), not a success firehose |
| 2026-09-16 | Custom LLM service architecture | Standalone service (Python/FastAPI/Pydantic), not n8n Code nodes — see `LLM-SERVICE-SPEC.md` |
| 2026-09-16 | Embedding provider | Voyage AI (`voyage-3-lite`/`voyage-3`) for pgvector embeddings |
| 2026-09-16 | Intake mechanism | n8n Form Trigger (native form + file upload) rather than a custom front-end for MVP |
| 2026-09-16 | Build order | Next up: Intake + Research & Retrieval + Source Curation, spec'd in `INTAKE-AND-RESEARCH-SPEC.md`, not yet built as workflow code |
| 2026-09-16 | Intake mechanism (revised) | Replaced n8n Form Trigger with a custom front-end (Next.js + Supabase) that talks to n8n over webhooks — reused as the Human Review UI too, not just intake. See `FRONTEND-SPEC.md`. Supersedes the 2026-09-16 "Intake mechanism" entry above. |
| 2026-09-16 | Multi-reviewer race handling | Moved from an app-level "first wins" check to a database unique constraint (`review_decisions`), closing the check-then-act race window the earlier design still had |
| 2026-09-16 | Front-end scaffold built | Card/Kanban board direction chosen (over a table or a simple list), neutral palette for now, built as a full runnable Next.js scaffold against seeded mock data covering both flows (submit request, review dashboard) rather than a static mockup — see §21 and `frontend/README.md` |
| 2026-09-16 | Pipeline moved into Next.js, off n8n + standalone service | Research, curation, planning, drafting, evaluation, revision, and channel prep all now run as Next.js server code in the same app as the front-end, calling Claude/Firecrawl/Tavily/Voyage directly instead of routing through an n8n workflow into a separate Python/FastAPI service. Reasoning: intake and review had already moved into this app (the 2026-09-16 "Intake mechanism (revised)" entry above); keeping the generation stages on the far side of an HTTP hop into a second service added a network boundary, a second deployment target, and a second auth/secret to manage for no benefit once nothing else was consuming that service — n8n's own value here was never "orchestrating Claude calls," it was the scheduler + platform API nodes it already provides for publishing. n8n's role is now exactly that: the publish runner (§11), unchanged. Supersedes §19 and §20; current spec is `CONTENT-PIPELINE-SPEC.md`. Built as real code, not just designed — see §22. |
| 2026-09-16 | Newsletter channel copy always generated | Channel Prep generates LinkedIn, X, and newsletter copy for every request that lists them as a priority channel, regardless of which channels the n8n publish runner currently automates. Newsletter generation was never blocked on distribution being decided — only *sending* it is deferred (§11 "Scope note", Decisions Log's original newsletter entry). Per-channel copy (including newsletter) is now visible in the review UI for every request that has reached channel prep, not only ones already approved. |
| 2026-09-16 | Stale-draft guard added to review-action | While wiring the pipeline's automatic revision rounds, found that `review-action` never checked whether `draft_id` was the request's current draft — a decision aimed at a since-superseded version would still silently move the request to approved/rejected. Fixed with an explicit check (`stale_draft`, HTTP 409), the same "gate, don't guess" pattern as the existing `already_decided` race guard, added once revisions started happening automatically and frequently enough to make a stale draft_id a real, not just theoretical, possibility. |
| 2026-09-16 | Seed data bug: req-5 had no real draft/source records | User-reported ("can't see newsletter content, can't read the drafts"). `seed.ts`'s `req-5` (the seeded "published" example) had zero entries in `seedDrafts` and `seedSources`, while its channel assets (`ca-2`, `ca-3`, and the newsletter asset) all pointed `draft_id` at `draft-3` — a draft that actually belongs to `req-3`. The draft panel correctly reported "no draft generated" for req-5 even though its channel copy rendered fine. Fixed by adding a real source (`src-6`) and draft (`draft-5`, passing evaluation `eval-5`) for req-5 and repointing its channel assets to `draft-5`. Also defaulted the new-request form's channel checkboxes to include Newsletter (previously only LinkedIn + X were pre-checked), since newsletter copy is meant to always be generated. |
| 2026-09-16 | Channel-copy truncation bug fixed | User-reported ("can't view a post content / like read it"). `prepareChannelAssets` built the LinkedIn/X "hook" text with a hard `.slice(0, 140)` off the draft's first section body — this cut mid-word with no ellipsis (e.g. "...decisions already. T"), making generated posts read as garbled/truncated. Replaced with a `truncateAtWord` helper that breaks on the nearest word boundary and appends "…", used for both the 140-char hook and X's overall length-limit truncation. Newsletter content was unaffected (never truncated). Verified live: a fresh request's LinkedIn/X copy now ends cleanly at a word boundary. |
| 2026-09-17 | PRD compliance audit: "select content" has no human-facing UI | Checked the implementation against the PRD's review-step line ("a human can approve, reject, revise, or select content before publishing"). Approve/reject/request-changes are all wired and gate publishing correctly — confirmed `publishing_queue` (real-mode schema, §7) only ever receives rows after an explicit approval, and the front-end's "Publish immediately after approval" / "Schedule" choice only sets *when* an already-approved item goes out, never whether it needs approval. But "select" has no equivalent for a human today: Planning's two title/angle options are picked by the pipeline's own heuristic (`planTitleOptions()`) before a human ever sees a draft — `VersionHistory` lets a reviewer browse past *revision* rounds, not choose between the original options. This is a real gap against the PRD's literal wording, not just a naming mismatch. Fixed same day — see the next two entries. |
| 2026-09-17 | Publish timing moved from intake to approval | User-requested: "Publishing option should not appear in new requests." The immediately/scheduled picker (with its datetime input and future-date validation) is removed from `NewRequestForm` entirely — a request now always starts `publish_timing: "immediately"`, `scheduled_for: null` regardless of what's posted to the intake webhook. The same picker reappears inside `ReviewActions`' Approve panel instead, since a request's fate isn't known at submission time and the PRD's own ordering is "review, THEN publish or schedule." `handleReviewActionWebhook` re-validates the schedule server-side on approve (`invalid_schedule`, HTTP 400) the same way intake used to — the validation itself didn't change, only which screen collects it and when it's applied to the request. |
| 2026-09-17 | "Select content" gap closed — ContentAngleSelector | Closes the audit finding above. Both of Planning's title/angle options (previously discarded once one was picked) are now persisted on the request (`title_options`, `chosen_title_index`, `title_option_reason`) and surfaced in a new `ContentAngleSelector` panel on the review screen. A reviewer can switch to the other angle any time before a decision is recorded (locked afterward, same as every other review action) — doing so calls a new `select_option` code path (`runSelectOptionPipeline` in `run.ts`) that regenerates a full draft under the new title through the same evaluate/auto-revise loop as a fresh submission, re-prepares channel copy from the result, and logs a human-attributed activity entry naming both the old and new angle. Verified live: switching req-1 from option A to B regenerated its draft and channel copy under the new title, and switching back worked the same way. |
| 2026-09-17 | Real-data cutover implemented (database + real AI/search) | User-requested, explicitly scoped to "Database + real AI/search too, but do not run any tests yet." Built, but deliberately **not executed against live infrastructure**: `supabase/schema.sql` (complete DDL matching §6/§7/§13, including the `review_decisions.draft_id` `UNIQUE` constraint and `pgvector` `source_chunks`); `src/lib/supabase/server.ts` (service-role client — real Auth still isn't wired up, so real-mode reads use this same client for now, same trust boundary the mock's "whole app acts as one profile" already had); real `src/lib/data.ts` reads; `src/lib/ai/{anthropic,firecrawl,tavily,voyage}.ts` (plain-`fetch` clients, no SDK deps beyond `@anthropic-ai/sdk`); `src/lib/pipeline/generate.real.ts` (the real research/plan/draft/evaluate/revise/channel-prep pipeline, every function assigning its own `crypto.randomUUID()` id so a child row's id is valid before either row is inserted — the same problem the mock's `nextId()` counter solves, just for Postgres); and `src/lib/supabase/webhooks.ts` (the real orchestrator, mirroring `mock/webhooks.ts` + `pipeline/run.ts` combined, including inserting a `publishing_queue` row on approval and using the real `review_decisions` `UNIQUE` constraint — catching Postgres error `23505` — as the actual multi-reviewer race guard instead of the mock's find-then-refuse check). `src/lib/webhookClient.ts` now routes real mode to this orchestrator directly instead of an HTTP call to n8n. `npm run typecheck` and `npm run build` both pass; mock mode (still the default) is untouched and unaffected. Not done: creating an actual Supabase project, obtaining live API keys, or running the pipeline end-to-end — see `frontend/README.md` "Going from mock to real" for the remaining setup steps. |
| 2026-09-17 | Two real-mode bugs found and fixed while answering "would this work without Tavily?" | (1) Tavily was accidentally a hard dependency for every request, not a supplementary one: `tavily.ts`'s `searchWeb()` threw on a missing `TAVILY_API_KEY`, and `researchAndCurateSources()` calls it unconditionally — so even a request submitted with its own `source_url` (which Firecrawl alone can fully research) would throw and land on `research_failed` if Tavily wasn't configured. Fixed to match Firecrawl's existing "one dead source shouldn't sink research" pattern: a missing key or failed request now logs a warning and returns `[]`, same as a single failed scrape. Firecrawl's own missing-key case is correctly left as a hard `throw` — it's the only thing that can actually retrieve page content, so there's no graceful degradation available for it. (2) While tracing that path, found a more consequential gap: `generateDraft`/`evaluateDraft`/`reviseDraft` only ever received each source's `id`/`title`/`url`/`relevance_note` — never the source's actual scraped text — so real-mode drafts were never actually grounded in retrieved material, only in a title and a one-sentence note about it, undermining exactly the PRD line audited two entries above ("the final content should stay grounded in reviewed source material"). Fixed: added `SourceRef.raw_text` (matching `schema.sql`'s already-present, previously-unused `sources.raw_text` column), populated from each scraped page's markdown, and threaded a bounded `excerpt` (`sourceExcerpt()`, 6000 chars/source) into all three prompts, plus into the evaluation call's `available_sources` so grounding/factual-consistency checks are against real text, not titles. `npm run typecheck` / `npm run build` re-verified clean; still not executed live. |
| 2026-09-17 | Real Supabase Auth wired up — admin-provisioned accounts + magic link | User-requested ("Lets wire up the supabase auth. the db has not been creat[ed] yet"), with the auth model chosen explicitly when asked: a user already in the system provisions new users and sets their role (no public self-serve signup), and sign-in is passwordless magic link. Implemented with `@supabase/ssr`, which requires three distinct client constructions for three different contexts: `src/middleware.ts` (its own client over `NextRequest`/`NextResponse` cookies — the only place a refreshed session actually gets persisted), `src/lib/supabase/routeClient.ts` (Server Components/Route Handlers, via `next/headers`' `cookies()`, with `set`/`remove` wrapped in try/catch since a Server Component render can't set cookies), and `src/lib/supabase/browserClient.ts` (the `/login` page only). `middleware.ts` gates every route except `/login` and `/auth/callback`, redirecting signed-out requests to `/login?next=<path>` and signed-in requests away from `/login`; it no-ops entirely when `USE_MOCK_DATA` is true, so mock mode is completely unaffected. `/login` (`LoginForm.tsx`) collects only an email and calls `signInWithOtp`; `/auth/callback` exchanges the emailed code for a session via `exchangeCodeForSession`. Provisioning is `POST /api/admin/users`: requires the caller's own profile to already include the `admin` role (checked via a new `src/lib/currentUser.ts`, `getCurrentProfile()`/`getCurrentProfileId()` — mock mode returns `MOCK_CURRENT_PROFILE_ID`, real mode reads the session), then calls the privileged `auth.admin.inviteUserByEmail()` (service-role client) and inserts the matching `profiles` row in the same request — there's no signup trigger, provisioning is the only way a `profiles` row gets created. `/admin/users` shows a mock-mode explainer instead of the real form when `USE_MOCK_DATA` is true. Every place that used to read `MOCK_CURRENT_PROFILE_ID` directly in real-mode code paths (`requests/new`, the `content-request` and `review-action` webhook routes) now calls `getCurrentProfileId()` instead, and refuses with 401 if it's null (shouldn't happen — middleware already gated the route — but refused explicitly rather than silently attributing a write to nobody). Schema change: `profiles.id` changed from a freestanding `default gen_random_uuid()` primary key to `references auth.users(id) on delete cascade`, since RLS's `auth.uid()` needs to resolve against real rows. This also **activates** the SELECT policies already written in `supabase/schema.sql` (submitted_by/reviewer/admin scoping) — they existed since the real-data cutover entry above but were inert because every real-mode read went through the service-role client, which bypasses RLS entirely. `src/lib/data.ts`'s `listRequests`/`getRequestDetail` now use a new session-bound `routeClient.ts` client instead, so those policies actually filter what a signed-in user sees; `getProfile`/`listProfiles` deliberately stay on the service-role client (`profiles` has no RLS at all, by design — the reviewer picker needs to see everyone). Known bootstrapping gap, documented in `frontend/README.md`: `/admin/users` requires an existing admin, and a fresh project has none, so the very first admin has to be created by hand (one manual `auth.users` + `profiles` insert) — there is no way around this without a public signup path, which the chosen model explicitly rules out. `npm run typecheck` and `npm run build` both pass, including new routes (`/login`, `/auth/callback`, `/admin/users`, `/api/admin/users`, `/api/auth/signout`) and an `87.2 kB` middleware bundle; **not executed against a live Supabase project** — same "built, not run" status as the rest of real mode, since no Supabase project exists yet. |
| 2026-09-17 | SEO Fit actually scored; custom rubric criteria addable at intake; tone accepts free text | User-requested, three related gaps closed together. **(1) SEO Fit**, flagged on the project checklist as "a flat placeholder score": mock mode's `evaluateDraft()` used to hardcode `score: 8` for every request regardless of content. Added `scoreSeoFit()` (`generate.ts`), which checks the draft against `assets/seo-best-practices.md`'s actual checklist — primary keyword (guessed deterministically from `idea_or_topic`, longest 1-2 non-stopword tokens) present in the title, present in the first ~100 words, an H1/H2-equivalent structure (title + 2+ section headers), and a 2+ link/citation count — scoring 0-10 by how many of those four checks pass, with notes listing which passed/failed. Verified via a throwaway `tsx` script exercising `generateDraft`/`evaluateDraft` directly (no dev server): a normal draft scored 8/10, a deliberately keyword-less/linkless one scored 2/10 — confirming the score now actually moves instead of always landing on 8. Real mode (`generate.real.ts`) previously only gave the evaluation prompt one line about SEO Fit and never the actual checklist or `idea_or_topic` — both are now included, so Claude scores it against the same guidelines draft generation already follows, instead of judging it loosely. **(2) Custom rubric criteria at intake**: `ContentRequest` gained `custom_rubric_criteria: {name, description}[]` (new `supabase/schema.sql` column, `jsonb`, default `[]`), settable from a new repeatable list on `NewRequestForm` (add/remove, capped at 5, validated server-side too in `validateNewRequest` — non-empty unique names ≤60 chars, non-empty descriptions ≤300 chars). `evaluateDraft` (both modes) now scores each one alongside the 9 defaults, tagged `scope: "custom"` — `EvaluationPanel` already rendered that scope with a ★ marker from the earlier seed data, so no UI change was needed there. Mock mode scores a custom criterion with a keyword-overlap heuristic against its own description (explicitly labeled as a heuristic in its notes — mock mode can't actually judge an open-ended criterion like "Hook Strength" the way a human or Claude would); real mode has Claude score it directly, appended after the 9 defaults in the same structured call, with the validator now checking for `9 + customCriteria.length` scores instead of a fixed 9. `req-1`'s seed data (which already had a "Hook Strength" custom score in its evaluations, added earlier without any at-intake origin) now carries the matching `custom_rubric_criteria` entry, and the request-detail sidebar shows a request's custom criteria list. This is still request-scoped, not the reusable channel/content-type-scoped registry DESIGN.md §13 originally sketched — that remains unbuilt (README.md "What isn't built yet"). **(3) Custom tone**: `NewRequestForm`'s tone field was a fixed 4-option dropdown with no way to enter anything else. `tone` was already a plain `string` everywhere downstream (types, validation, both pipelines), so no data-model change was needed — added an "Other (type your own)…" option that reveals a free-text input, and added server-side validation (non-blank, ≤40 chars) that didn't exist before for tone at all. `npm run typecheck` and `npm run build` both pass for all three changes; mock-mode logic was smoke-tested directly (no live infra involved, so no conflict with the standing "don't run live tests yet" constraint, which is about the real Supabase/AI integration specifically). |
| 2026-09-17 | Migrated off legacy Supabase anon/service_role key naming to publishable/secret keys | User asked why the auth code used the "anon key" pattern instead of the "publishable key." Checked Supabase's current docs: the legacy `anon`/`service_role` JWT keys are being deprecated by the end of 2026 in favor of `sb_publishable_...`/`sb_secret_...` keys, which are drop-in replacements at the same call sites (same `createClient(url, key)` signature, no library upgrade needed) — the publishable key carries the anon key's same low/public privileges, the secret key the service_role key's same full-privilege role. Since nothing has been run against a live Supabase project yet, this was a zero-risk time to switch rather than migrating later under a deadline. Renamed throughout: `NEXT_PUBLIC_SUPABASE_ANON_KEY` → `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` and `SUPABASE_SERVICE_ROLE_KEY` → `SUPABASE_SECRET_KEY` (`.env.example`, `src/lib/config.ts`'s exported consts, `browserClient.ts`, `routeClient.ts`, `middleware.ts`, `server.ts`, and doc comments/README referencing either by name). `.env.example` now explicitly flags that Supabase's dashboard still shows the legacy keys under a separate "Legacy API Keys" section — the new ones are the current keys under Settings -> API Keys, not a special/advanced option. No behavior change: this is a rename of which key material flows through the same client constructions, not a change to the auth model, RLS, or trust boundaries. `npm run typecheck` and `npm run build` both pass. |
| 2026-09-18 | Fixed live RLS infinite-recursion bug (42P17) on requests/request_reviewers | First bug surfaced by an actual live Supabase project (schema.sql run for real, magic-link sign-in attempted): loading `/board` threw Postgres `42P17`, `"infinite recursion detected in policy for relation \"requests\""`. Root cause: `requests_select` and `request_reviewers_select` each subqueried the OTHER table directly inside their `USING` clause; since both tables have RLS enabled, evaluating either policy re-triggered the other's policy, which re-triggered the first again, indefinitely. Confirmed against Supabase's current RLS docs that the standard fix is to route the cross-table check through a `SECURITY DEFINER` SQL function — such a function runs as its owner (the table owner, who bypasses RLS by default), so the query *inside* it doesn't re-trigger the other table's SELECT policy, breaking the cycle. Added `is_request_reviewer(request_id, profile_id)` and `is_request_submitter(request_id, profile_id)` (both `security definer`, `set search_path = ''` — Supabase's mandatory hardening for any `SECURITY DEFINER` function referenced from RLS, since an unqualified search path lets a caller shadow a referenced table/object; also applied the same hardening to the pre-existing `is_admin()` while touching this section) and rewrote both policies to call them instead of subquerying directly. `supabase/schema.sql` now has the fixed version for any *fresh* project. Since the user's project already ran the old script, schema.sql alone doesn't retroactively fix it — added `supabase/fixes/2026-09-18-fix-requests-rls-recursion.sql`, a standalone idempotent snippet (`drop policy if exists` before recreating, `create or replace function` for the functions) to paste into that project's SQL Editor directly. User confirmed running it against their live project fixed `/board`. |
| 2026-09-18 | Closed a real self-signup security gap; explained the resulting "no New Request button" symptom; added back-nav to the new-request screen; clarified Redirect URLs for local vs. production | Three items reported together, the first two turning out to be the same root cause. **(1) Self-signup bypass:** the user was able to type an email that had never been provisioned via `/admin/users` into `/login` and receive a working magic link — a real hole against the documented "admin provisions accounts, no public signup" model (2026-09-17's auth entry above). Cause: `supabase.auth.signInWithOtp()` was called with no `shouldCreateUser` option, which defaults to `true` — Supabase silently creates a brand-new `auth.users` row for any email typed into that form, accounts model in the code comments notwithstanding. Fixed by passing `shouldCreateUser: false` explicitly (`LoginForm.tsx`); an unprovisioned email now gets a clear in-form message instead of a magic link. **(2) "+ New Request" button missing:** direct consequence of (1) — the account the user tested with got an `auth.users` row via the bypass above but never got a matching `profiles` row (that only happens through `/admin/users`' `POST /api/admin/users`, which does both atomically). `RootLayout` gates "+ New Request" (and everything else) on `profile` being non-null, so a signed-in-but-profile-less session just silently lost every nav item with no explanation. Rather than only closing the hole going forward, added a real state for this: `layout.tsx` now resolves `profileId` and `profile` separately so it can tell "no session" apart from "session, no profile row," and when the latter is detected it renders a plain "Account not set up yet — ask an admin to add you" message in place of the page instead of a half-working shell (also surfaces the sign-out button in that state, so they can try a different email). README's bootstrap-admin section now calls this out directly as the likely cause if it's seen, with the one-line `profiles` insert to run for an already-existing `auth.users` row. **(3) Back navigation on `/requests/new`:** had no way back to the board except the browser's own back button. Added the same `← Back to board` `Link` the request-detail page already uses, for consistency. **(4) Base URL / Redirect URLs for local vs. production:** checked — `LoginForm.tsx` and `/api/admin/users/route.ts` already build their redirect URLs from `window.location.origin`/`req.nextUrl.origin` respectively, so the app itself never hardcodes a host and already does the right thing in both environments automatically. The actual gap was operational, not code: Supabase's **Redirect URLs** allowlist has to separately include both `http://localhost:3000/auth/callback` and the Vercel production `.../auth/callback`, or a request from whichever origin isn't listed silently falls back to the single **Site URL** setting instead. README's cutover steps now spell this out explicitly (Site URL = production, both origins in Redirect URLs, optional preview wildcard) rather than only showing one example origin. `npm run typecheck` and `npm run build` both pass. |
| 2026-09-18 | Sign-in now checks account existence explicitly, before ever sending a magic link | User reported the magic link was still going out after the `shouldCreateUser: false` fix. Rather than rely on that flag alone, sign-in moved server-side: `POST /api/auth/request-magic-link` looks the email up in `profiles` (secret-key client, case-insensitive) first — no row, no email, just a 404 with a clear message — and only calls `signInWithOtp` (still with `shouldCreateUser: false`) once a matching account is confirmed. `LoginForm.tsx` now posts to this route instead of calling Supabase directly from the browser; `src/lib/supabase/browserClient.ts` is deleted (nothing else used it). Also removed the "accounts are provisioned in-app, not self-service" line from the login screen and shortened it to a plain "Contact your administrator" — that phrasing read as internal implementation detail, not end-user copy, on what is a production app. `npm run typecheck` and `npm run build` both pass. |
| 2026-09-18 | Request submission no longer blocks on the full pipeline; real "in progress" state added | User asked what happens between submitting a request and it being ready, having noticed there was no real pending state — just a disabled "Submitting..." button for however long research/drafting/channel-prep actually took (low tens of seconds to a couple of minutes against real Firecrawl/Tavily/Claude/Voyage calls). Two problems: a poor experience (nothing to look at for that whole span), and a real production risk — no `maxDuration` was set anywhere, so the route was running on Vercel's short default serverless timeout, well under the pipeline's actual runtime; a slow research step could 504 and look like the app was broken. Fixed by splitting `handleContentRequestWebhook` (`src/lib/supabase/webhooks.ts`): the fast part (insert the request row, reviewers, activity log) is still awaited and responded to immediately; the slow part (`runInitialPipeline`) is handed to `waitUntil` (`@vercel/functions`, new dependency) instead of being awaited on the request path, so the function returns right away but Vercel keeps it alive to finish the pipeline afterward. Added `export const maxDuration = 300` to the route to give that background work real time to run (raise further if a Vercel plan's ceiling allows and runs still time out). Added a real in-progress UI: `PipelineProgress.tsx` (new, client component) renders on `/requests/[id]` whenever status is `intake_complete`/`research_complete`/`drafting`, showing which step is running and polling via `router.refresh()` every 4 seconds until the status moves to something terminal — the same page just updates itself instead of the user needing to know to refresh. Also closed a gap the old synchronous design got away with: an error thrown after the research step (drafting, evaluation, channel prep) used to propagate as an uncaught rejection straight to the HTTP response; now that nothing awaits the pipeline, an uncaught error there would have silently orphaned the request at whatever status it last reached, with the UI polling forever and no failure ever shown. `runInitialPipeline`'s caller now wraps it in a `.catch()` that calls `failResearch` (renamed in spirit, not in code — it's the generic pipeline-failure handler now, not just a research-step one) so any late failure still lands on `research_failed` with an accurate reason instead of hanging. Mock mode is untouched — its pipeline is synchronous and instant already (no real I/O to background), so `PipelineProgress` simply never has anything to show there. `npm run typecheck` and `npm run build` both pass; not yet verified against a real Vercel deployment (background execution past the response can't be exercised by a local `next build`/`next dev` alone). |
| 2026-09-18 | n8n retired; publishing reverts to queue-only; Discord notifications move into the app for generation events | User-requested: "we would not be going with the n8n delivery workflow" — the automated LinkedIn/X publish runner (`n8n-publish-workflow.json`) is dropped, not replaced. `publishing_queue`, populated on approval exactly as before, is itself what satisfies the PRD's "publish or schedule for release" bar (§11, revised) — this reverts the 2026-09-14 "Publishing scope (revised)" decision, back to the original queue-only scope from before n8n's automation was added. No poller, no platform API calls exist anywhere in the system now. `n8n-publish-workflow.json` and `PUBLISH-WORKFLOW-SETUP.md` are kept for history (the setup doc now carries a retirement banner); §11 does the same. Also fixed two comments (`webhookClient.ts`, `src/app/api/webhooks/content-request/route.ts`, `src/lib/mock/webhooks.ts`) that still described intake as calling through n8n — stale since the 2026-09-16 pivot to Next.js server code, and actively wrong now that n8n has no role left at all. Second half of the request: n8n's Discord notifications (publish failed, stale, repeated failures) don't carry over as-is, since there's no automated publishing left to alert on — instead, new notifications fire from the generation side, which n8n never covered: `src/lib/discord.ts` (new, a single `notifyDiscord()` that POSTs to `DISCORD_WEBHOOK_URL` and no-ops if it's unset) is called from `src/lib/supabase/webhooks.ts` when a request's initial draft is ready for review (✅, with a link built from the request's own origin — threaded through from the API route since there's no incoming request in the backgrounded `waitUntil` context to read `origin` from) and when the pipeline fails at any stage (⚠️, via the same `failResearch` catch-all the async-pipeline change above added). Deliberately scoped to just the initial submission path for now — human-triggered regeneration (switching title options, a reviewer's "request changes" producing a new revision) isn't wired up to Discord yet, since a human is already in the app watching those; easy to add the same way later if wanted. `npm run typecheck` and `npm run build` both pass; not yet verified with a real Discord webhook URL configured. Also checked, per the user's reminder in the same message: real-mode generation already follows both `assets/seo-best-practices.md` and `assets/channel-formatting-rules.md` — `generate.real.ts`'s `SEO_GUIDELINES` and `CHANNEL_FORMATTING_RULES` constants (added 2026-09-17, §"SEO Fit actually scored...") are condensed from those exact files and are already threaded into `generateDraft`, `evaluateDraft`, and `prepareChannelAssets`'s prompts — no gap found, nothing changed there. |
| 2026-09-18 | One blocked/unreachable source during research was killing the entire request | User-reported, hit live: submitting a request whose search results included a Reddit URL Firecrawl refuses to scrape ("we do not support this site", HTTP 403) sent the whole request to `research_failed` with `Research step failed: TypeError: fetch failed`, even though other candidate sources would have scraped fine. Root cause: `scrapeUrl` (`src/lib/ai/firecrawl.ts`) and `searchWeb` (`src/lib/ai/tavily.ts`) already treated a non-OK HTTP response as "this one failed, return null/[]" (their own doc comments say so), but neither wrapped the `fetch()` call itself in a try/catch — a network-level failure (DNS, connection reset, TLS, a host refusing the connection outright) throws from `fetch()` before any response exists, escaping both functions uncaught. `researchAndCurateSources`'s `Promise.all` over every candidate URL is fail-fast, so one such throw failed the entire batch instead of just that one source — exactly what happened with the Reddit URL. Fixed by wrapping both functions' fetch calls in try/catch, catching and logging any failure (network-level or HTTP-level) and resolving to `null`/`[]` either way, matching what both functions' own doc comments already promised. Also switched the scraping fan-out from `Promise.all` to `Promise.allSettled` as cheap insurance against this exact class of bug recurring even if a future change reintroduces a throw somewhere in the chain. Noted but not fixed in this pass: `prepareChannelAssets`'s `Promise.all` over channels (LinkedIn/X/newsletter) has the same fail-fast shape if the Anthropic API call itself throws for one channel (rather than returning malformed content, which `structuredCall`'s retry already handles) — lower risk (Anthropic's API is more reliable than scraping arbitrary sites, and this would only run after a draft already passed evaluation, i.e. after most of the real cost is already spent), but the same fix pattern would apply if it's ever hit live. `npm run typecheck` and `npm run build` both pass. Also added, per a separate request in the same conversation: a one-time "✅ Request submitted" banner on `/requests/[id]` (shown via a `?submitted=1` query param set by `NewRequestForm`'s redirect) telling the submitter they can close the page or come back later — replacing the previous silent navigate-and-hope with no confirmation at all. |
| 2026-09-18 | Clicking a request card still 404'd after the direct-URL case was confirmed working | User reported this persists even after a direct link to a request detail page worked. `listRequests()` (the board) and `getRequestDetail()` (the detail page) both already query `requests` through the same RLS-scoped `routeClient` — same table, same policy, same session — so a row visible on the board should be equally visible by id, ruling out an RLS/permissions mismatch between the two. The remaining likely cause: the App Router's client-side Router Cache, which is separate from (and not disabled by) the `force-dynamic` already set on both routes — it can serve a previously-cached result for a dynamic route for up to 30 seconds after a client-side `<Link>` navigation, independent of the server always rendering fresh. On a board where a card's status changes within seconds (especially now that the pipeline runs in the background), this is plausibly stale enough to explain "the board shows it current, but clicking into it 404s." Set `experimental.staleTimes.dynamic = 0` in `next.config.mjs` — every client-side navigation to a dynamic route now always fetches fresh from the server, matching what `force-dynamic` already intended. Requires restarting `npm run dev` (or a fresh Vercel deploy) to take effect — a `next.config.mjs` change isn't picked up by a running dev server. Not yet confirmed this fully resolves it; if it recurs after restarting, the next things to check are: does a hard reload (not a `<Link>` click) on the exact 404'd URL also fail, and is it a specific request (e.g. one still mid-pipeline) or every request. |
| 2026-09-18 | Found and fixed the real cause of the persistent `/requests/[id]` 404 — `getRequestDetail` was 404ing the whole page over a missing submitter profile join | The router-cache theory above was disproven by the user's own evidence: a confirmed-fresh dev server compile (`✓ Compiled /requests/[id]`, implying a restart, so `staleTimes.dynamic = 0` was in effect) still produced a 404 on a direct `GET`, and the response took 10.5 seconds to resolve — far longer than the single fast query the *first* null-check in `getRequestDetail` (`!requestRow`, an immediate `return null` on a genuinely missing/RLS-denied row) would take. That timing is the tell: it means the code was reaching the *second* null-check, after the `requests` row was already found and several more queries (submitter profile, reviewer profiles, sources, drafts, channel assets, activity log, then evaluations/decisions) had run. `getRequestDetail` had `if (!submitterRow) return null;` — if the `profiles` row for `request.submitted_by` doesn't exist, the entire request page 404'd even though the request itself was found and fully visible to the viewer. That turned a cosmetic "unknown submitter" data gap into "I can't see my own request." Fixed in `src/lib/data.ts`'s `getRequestDetail`: a missing submitter profile no longer returns `null` for the whole page — it logs a `console.error` naming the request id and the dangling `submitted_by`, and renders with a placeholder submitter (`{ name: "Unknown user", email: "", roles: [] }`) instead; the query is also skipped entirely (rather than run with a falsy id) when `submitted_by` itself is empty. The first null-check (`!requestRow`, a real not-found-or-RLS-denied case) is untouched and still 404s correctly, now with its own `console.warn` so the two paths are distinguishable in logs. `npm run typecheck` and `npm run build` both pass. |
| 2026-09-18 | Confirmed live: the dangling `submitted_by` is a real, current data-integrity gap, not stale history — root cause and fix documented, live DB fix pending user action | The placeholder fix above was initially guessed to be explaining old test data. The user then reproduced it live on a **brand-new** request (`submitted_by` pointing at a profile row that doesn't exist, confirmed via the new `console.error`, with Firecrawl actively scraping in the same log — i.e. this just happened, not historical). That's significant: `schema.sql` defines `requests.submitted_by` as `uuid not null references profiles(id)` — a NOT NULL foreign key — so an insert naming a `submitted_by` with no matching `profiles` row should be physically impossible if this live project's schema actually has that constraint. Traced every write path (`handleContentRequestWebhook` in both `mock/webhooks.ts` and `supabase/webhooks.ts`, the only two places that ever set `submitted_by`, confirmed by `grep`) — both set it once, at insert, from the API route's `getCurrentProfileId()`-derived, 401-guarded value; no code path ever sets it to null or a bad value. So the gap isn't in this app's code — it's either (a) an orphaned `auth.users` row (someone can hold a session as that id with no matching `profiles` row — most likely a leftover from the self-signup bug closed earlier today: an account created via the `shouldCreateUser` bypass before it was patched, still signed in and submitting requests, since nothing about *submitting* a request ever checks that the signed-in id has a profile, only that it's non-null), or (b) this live project's `requests` table predates the NOT NULL + FK constraint `schema.sql` currently specifies (same class of drift as the 2026-09-18 RLS-recursion fix needed for the same reason). Added `supabase/fixes/2026-09-18-backfill-orphaned-profiles.sql` — read-only diagnostic queries first (does the constraint exist on the live table; list every `auth.users` row with no matching `profiles` row), then the actual fix (insert the missing `profiles` row(s) for whichever id(s) step 2 finds, then optionally add the constraint if step 1 came back empty). Not yet run against the user's live project — this is the concrete next action needed to close out the underlying gap (the app-level placeholder fix above means it no longer breaks the page in the meantime). |
| 2026-09-18 | Added a Retry button for `research_failed` requests, on both the board card and the request detail page | Direct follow-on to the 404 investigation above — the user needed a way to get a stuck failed request moving again without resubmitting a duplicate. New `handleRetryRequestWebhook` (mirrored in `mock/webhooks.ts` and `supabase/webhooks.ts`, wired through `webhookClient.ts`'s `submitRetryRequest` and a new `POST /api/webhooks/retry-request` route, same `waitUntil`/`maxDuration=300` background-execution shape as the initial submit): refuses anything not currently `research_failed` (`reason: "not_failed"`, 409), otherwise clears that failed attempt's partial rows (`sources`/`drafts`, which cascade to `source_chunks`/`evaluations`/`channel_assets`/`review_decisions`/`publishing_queue` via `schema.sql`'s `on delete cascade` — mock mode does the equivalent store filtering by hand), resets the request to `intake_complete` with `failure_reason`/`title_options`/`publish_timing`/`scheduled_for` all cleared, logs a new `retry_pipeline` activity entry, and re-runs the initial pipeline from scratch via `waitUntil`, exactly like a fresh submission. Deliberately restarts from the top rather than resuming mid-stage — `failResearch` is a generic catch-all for any stage failing (not just research), so a failed request can already have partial rows from drafting/evaluation/channel-prep by the time it reaches `research_failed`; resuming would risk duplicate/orphaned rows (e.g. two "version 1" drafts) without a real per-stage checkpoint system, which is out of scope here. New `RetryRequestButton` client component (`src/components/review/RetryRequestButton.tsx`) renders a full-size button in the request-detail page's failure banner and a `compact` variant on `RequestCard` — the card variant calls `preventDefault`/`stopPropagation` since the whole card is a `<Link>` to the request. Added `retry_pipeline` to the `ActivityAction` union and `ActivityTrail`'s label map. `npm run typecheck` and `npm run build` both pass; not yet exercised against a real failed request in the live project. |
| 2026-09-18 | Removed internal DESIGN.md/spec section citations that had leaked into user-facing copy | User flagged one specific instance — the `research_failed` banner's "This request stayed isolated — it did not affect any other request in the pipeline (DESIGN.md §15)" — and asked for "any other message like this" to go too. Audited every `DESIGN.md §`/`SPEC.md §` occurrence in the codebase and removed the ones actually reaching an end user (internal code comments citing spec sections are fine and were left alone): the research-failed banner itself (`requests/[id]/page.tsx`, replaced by the new Retry button above rather than restored), the multi-reviewer helper text on the new-request form (`NewRequestForm.tsx`, "...first decision recorded wins (DESIGN.md §7)" → drops the citation), the "prepared channel-specific copy" activity-log note (`pipeline/run.ts` and `supabase/webhooks.ts` — both write into `activity_log.notes`, which `ActivityTrail` renders verbatim to reviewers — trimmed "...— see DESIGN.md §11" off the end), and the mock-mode newsletter draft body's footer line (`pipeline/generate.ts` and `mock/seed.ts`'s matching seed data — "...not yet distributed — see DESIGN.md §11" → "...not yet distributed", since that footer is literally shown as part of the generated draft content in `ChannelAssetsPanel`). Real-mode channel prep (`generate.real.ts`) was already clean — its content comes straight from Claude with no hardcoded suffix. `npm run typecheck` and `npm run build` both pass. |
| 2026-09-18 | `publish_timing` no longer defaults to "immediately" at intake — stays null until a reviewer actually approves | User-reported: a request's "Publish timing" showed "Immediately" the moment it was submitted, before any reviewer had looked at it — misleading, since nothing had actually been decided yet. Root cause: `requests.publish_timing` (`schema.sql`) was `not null default 'immediately'`, and both intake handlers (`mock/webhooks.ts`, `supabase/webhooks.ts`) explicitly set it to `"immediately"` on every new request "regardless of what the form sends" (the 2026-09-17 decision already correctly moved the *real* decision to `handleReviewActionWebhook`'s approve action — the leftover intake default was the bug, not the approval-time logic, which was already correct). Fixed: `ContentRequest["publish_timing"]` (`types.ts`) is now `"immediately" | "scheduled" | null`; both intake handlers set it to `null`; `schema.sql`'s column dropped its `not null default 'immediately'` (the existing CHECK constraint already tolerates null); a live-project fix (`supabase/fixes/2026-09-18-publish-timing-nullable.sql`) drops the same default/not-null on an already-provisioned database and optionally backfills any not-yet-approved request's stale `"immediately"` back to null. The request-detail page's "Publish timing" field now reads "Not yet decided — set on approval" for null instead of falling back to "Immediately". The new Retry button (above) also resets `publish_timing`/`scheduled_for` to null when retrying a failed request, self-healing any row still carrying the old default. Mock seed data (`mock/seed.ts`) updated to match: only `approved`/`published` example requests keep a real `publish_timing` (one of each — "scheduled" and "immediately" — to keep both UI branches exercised); every earlier-stage example is now null. `npm run typecheck` and `npm run build` both pass. |
| 2026-09-18 | Kanban card no longer dumps a failed request's full raw error text | User-reported, with a real example: a Voyage AI 429 rate-limit error (`failure_reason` = "Research step failed: Error: Voyage embeddings request failed: 429 {...}", including Voyage's full billing-page JSON response) was rendering in full on `RequestCard`, which has no length limit on that field — blowing out the card's height in its Kanban column. `failure_reason` was never guaranteed to be a short code (`no_search_results`) — `failResearch`'s generic catch-all embeds whatever `String(err)` produces, which can be arbitrarily long. Fixed in `RequestCard.tsx`: new `summarizeFailureReason()` hard-truncates to 100 chars with an ellipsis (kept as a hard cap rather than relying on `line-clamp` alone, since a long unbroken token — a URL, a run-on JSON blob — can defeat line-based wrapping), plus `line-clamp-2` as a second line of defense, plus the untruncated text in a `title` attribute so hovering still shows it in full. The request detail page's failure banner (`requests/[id]/page.tsx`) is deliberately left showing the full reason — that's the "drill in for detail" view, with room and the Retry button beside it — just changed `items-center` to `items-start` and added `break-words` so a long wrapped error doesn't look oddly centered against the button or overflow its container. `npm run typecheck` and `npm run build` both pass. Aside, worth flagging separately: the example error itself is a real, currently-live issue — Voyage AI's account has no payment method on file, capping it at 3 requests/minute and 10K tokens/minute, which is low enough to plausibly fail research on any request needing more than a couple of embedding calls; adding a payment method (Voyage still applies its free-tier token allowance afterward) would resolve it, independent of this UI fix. |
| 2026-09-18 | Unsupported-site scrape failures: skip known domains cheaply, measure the rest in Supabase, before deciding whether a fallback scraper/proxies/an official API is worth it | Follow-up to the live Firecrawl "we do not support this site" incident (Reddit) fixed earlier today with `Promise.allSettled` — that fix stopped one blocked source from failing the whole request, but didn't address the block itself. User shared a suggestion (residential proxies, a second scraping tool, stealth headless browsers) to bypass it; talked through first — flagged that "we do not support this site" is Firecrawl's own policy refusal (its 403 body says so explicitly), not a bot-detection wall, so proxies/stealth tactics wouldn't actually help, and that deliberately bypassing a platform's anti-scraping policy (Reddit/Instagram/Threads all actively enforce theirs) is a real ToS/legal-risk decision for a production tool, not just an engineering one. Agreed direction: skip known-unsupported domains cheaply with no new vendor, and measure actual frequency in Supabase before committing to anything heavier. Implemented: (1) `scrapeUrl` (`ai/firecrawl.ts`) now returns a tagged `ScrapeResult` (`{ok:true, page}` or `{ok:false, reason, detail}`, `reason` one of `unsupported_site` | `http_error` | `network_error`) instead of a bare `ScrapedPage | null`, detecting Firecrawl's specific refusal by its 403 body text rather than lumping it in with every other failure. (2) `researchAndCurateSources` (`generate.real.ts`) pre-filters candidate URLs against `KNOWN_UNSUPPORTED_DOMAINS` (`reddit.com`, `instagram.com`, `threads.com`, `threads.net` — everything actually observed refusing so far) *before* the up-to-6 scrape slots are chosen, so a known-dead domain doesn't cost a slot a real source could have used; both pre-filtered and live scrape failures are collected into a new `scrapeFailures` return value. (3) `runInitialPipeline` (`supabase/webhooks.ts`) persists `scrapeFailures` via new `logScrapeFailures` into `stage_logs` — an existing-but-previously-unused table already in `schema.sql` for exactly this ("machine-facing debug", as its own header comment already said) — one row per failure (`stage: 'research_scrape'`, `status: <reason>`, `error_message: '<url>: <detail>'`), regardless of whether the request ultimately succeeds or fails, so the measurement isn't biased toward only-total-failures. `stage_logs` had no RLS at all (the only table besides the intentionally-exempt `profiles`) — added `alter table stage_logs enable row level security` + an admin-only `stage_logs_select` policy, matching the security posture of every other table, since this is debug data no submitter/reviewer has a reason to see. Live-project fix: `supabase/fixes/2026-09-18-stage-logs-rls-and-scrape-telemetry.sql` (creates the table if an earlier schema.sql run predates it, adds the same RLS, includes two example queries — failure-reason breakdown, and which specific domains are being refused, to inform whether `KNOWN_UNSUPPORTED_DOMAINS` needs another entry later). Mock mode untouched — it doesn't do real scraping, so there's nothing to simulate here (same reasoning as `research_failed` being real-mode-only). `npm run typecheck` and `npm run build` both pass; the live-project SQL fix hasn't been run yet, so no real telemetry exists to review yet. |
| 2026-09-18 | Fixed a critical wrong-device-path bug: an unknown number of prior fixes this session were committed to a stray root-level `src/` instead of `frontend/src/` | User reported "the password tab is not showing" after the password sign-in feature had supposedly already been committed. Investigated with `device_list_dir` against the actual connected-device filesystem rather than trusting the paths used in prior `device_commit_files` calls, and found the real, runnable Next.js project (`package.json`, `node_modules`, `.next`) lives under a `frontend/` subfolder of the connected root (root-level spec docs like this one are siblings of `frontend/`, not inside it) — but every device commit in the relevant part of this session had been writing to `<root>/src/...`, missing the `frontend/` segment entirely, silently creating a parallel, non-functional `src/` tree the running dev server never reads. Confirmed via each affected file's `mtimeMs` that recent fixes (the magic-link PKCE fix, the premise check, password login, the middleware update) had genuinely never reached `frontend/src/...`. Fixed by re-committing every affected file to the correct `frontend/`-prefixed path, verifying each write's returned `devicePath`. Going forward, every device commit in this session double-checks the path includes `frontend/` before writing. The stray root-level `src/` folder this bug created was left in place (not yet cleaned up — offer still open to the user) since `device_bash` wasn't available at the time this was found. |
| 2026-09-18 | Full UI revision, round 1: `chosen_draft_id` decouples "the active draft" from "highest version number," so switching content angles reuses an existing draft instead of regenerating it | User's UI-audit note: "What actually happens when I click 'Use this instead'? it looks like it regenerates, even when it has been generated already. adding an extra ai call and an extra draft." Confirmed in code: `runSelectOptionPipeline` (both modes) unconditionally called `generateDraftWithAutoRevision` on every angle switch, and `DraftReviewPanel`/`ReviewActions` hard-assumed "the latest draft by version number" was the only one review actions could ever apply to. Asked the user how switching back to an already-generated angle should behave; their answer rejected both options offered ("copy content into a new version" and "warn before regenerating") in favor of a third: "You do not need to copy it content, The version already exist. switch to it." That requires an explicit "which draft is active right now" pointer independent of version number, since a reused draft can be an older version than the request's most recent one. Added `requests.chosen_draft_id` (nullable FK to `drafts`, `on delete set null`) — the pointer `ReviewActions`, `DraftReviewPanel`'s decision lookup, and `ContentAngleSelector`'s lock state now key off, in place of `drafts[drafts.length - 1]`. `runSelectOptionPipeline` (`pipeline/run.ts` and `supabase/webhooks.ts`) now looks for an existing draft whose `option_label` matches the target angle first: if one exists, it just repoints `chosen_draft_id` at it (no new draft row, no AI call, logged as a new `switch_to_existing_draft` activity action) — only an angle that's never been drafted for this request falls through to the original generate/evaluate/auto-revise path. This also exposed that `option_label` was hardcoded to `"A"` on every draft regardless of which title/angle it was actually generated for (a pre-existing, silent bug — `reviseDraft` correctly *preserved* it across revisions, but nothing ever set it to `"B"` in the first place); fixed by threading a real `optionLabel` parameter through `generateDraft` (mock and real) from whichever index was actually chosen. `channel_assets` also stopped being deleted-and-recreated wholesale on every regeneration/switch (`replaceChannelAssets` renamed to `insertChannelAssetsForDraft`, no delete) — since they're already keyed by `draft_id`, a reused draft's copy from when it was first active is simply still there, untouched. The stale-draft guard in `handleReviewActionWebhook` (both modes) now compares against `chosen_draft_id` instead of re-deriving "current" from the highest version number, for the same reason. Schema: `drafts.generated_by`'s CHECK constraint gained `'human_edited'` (see next entry) and `requests.chosen_draft_id` was added after the `drafts` table definition in `schema.sql` (it references `drafts(id)`, which didn't exist yet at `requests`' original position in the file). Live-project fix: `supabase/fixes/2026-09-18-chosen-draft-id.sql` (adds the column, updates the CHECK constraint, backfills every existing request's `chosen_draft_id` to its current highest-version draft so nothing changes for a request that's never had an angle switch, and flags any request the backfill couldn't resolve). `npm run typecheck` and `npm run build` both pass; smoke-tested the mock app's `/board` and `/requests/[id]` pages against a dev server with no runtime errors. |
| 2026-09-18 | Full UI revision, round 1: inline, versioned section editing | User: "I should be able to edit sections manually," resolved via `AskUserQuestion` to "inline edit, but versioned" (over "inline edit in place" and "dedicated edit mode"). New `handleManualEditWebhook` (mirrored in `mock/webhooks.ts` and `supabase/webhooks.ts`, wired through `webhookClient.ts`'s `submitManualEdit` and a new `POST /api/webhooks/manual-edit` route) takes a section index and its new body, copies the request's *active* draft's sections with just that one body replaced, and inserts it as a brand-new draft version tagged `generated_by: "human_edited"` — never mutates the existing draft row, matching the "versions always increment, never overwritten" rule every other draft-producing path in this app already follows. No AI call, no re-evaluation, no channel-copy regeneration — purely a text substitution — and repoints `chosen_draft_id` at the new version so it's immediately what `ReviewActions` applies to. Locked the same way approve/reject/request-changes already are: refuses (`reason: "locked"`, HTTP 403) once a decision has been recorded against the active draft, and refuses (`reason: "stale_draft"`, HTTP 409) if the draft the client was looking at isn't the request's current active one anymore. `DraftViewer.tsx` became a client component to support this: each section gets an inline "Edit" affordance (only rendered when `DraftReviewPanel` passes `editable` — true only for the active, undecided draft; historical or already-decided versions stay read-only) that swaps the section's body for a textarea, with a "Save (creates a new version)" button making the versioning explicit rather than implying an in-place edit. `Draft["generated_by"]` and the `drafts.generated_by` CHECK constraint both gained `"human_edited"`; `VersionHistory` labels it "Manually edited"; `ActivityAction` gained `edit_section` ("manually edited a section"). `npm run typecheck` and `npm run build` both pass. |
| 2026-09-18 | Full UI revision, round 1: channel copy moved into a Draft / Channel Copy tab layout | User: "The channel copy should should not be all the way at the bottom of the page," resolved via `AskUserQuestion` to tabs (over "move higher in the same column" and "move to the right sidebar"). New `RequestTabs` client component wraps the request-detail page's draft column (angle selector + `DraftReviewPanel`) and the channel-copy card as two tabs instead of one long stacked column; both panes stay mounted (toggled with `hidden`, not unmounted) so switching tabs never resets `DraftReviewPanel`'s own selected-version state. The Channel Copy tab now also only shows the *active* draft's channel assets (`channelAssets.filter(c => c.draft_id === activeDraft.id)`) rather than every channel asset ever created for the request — a direct consequence of no longer deleting old channel assets on regenerate/switch (previous entry): without this filter, a request that had switched angles or been revised a few times would show several drafts' worth of LinkedIn/X/newsletter copy stacked together. |
| 2026-09-18 | Full UI revision, round 1: activity trail shows newest first; `ContentAngleSelector` and `ReviewActions` no longer imply a switch/decision is instant; `AddUserForm` stale-list bug fixed | Three smaller items from the same UI-audit note. (1) "Activity should show last first" — `getRequestDetail`'s activity sort (mock and real) flipped from ascending to descending by `created_at`; purely a display-order change, no schema/type impact. (2) `ContentAngleSelector` now tells a reviewer which angle already has a generated draft (`optionsHaveDrafts`, computed server-side from `drafts.some(d => d.option_label === "A"/"B")`) and labels the action "Switch to this" vs. "Generate this instead" accordingly, with per-button "Switching…"/"Generating…" loading text while a request is in flight — directly closes the "it looks like it regenerates" confusion this whole round started from, now that the two cases are visibly different. `ReviewActions`' confirm buttons gained the same present-participle-while-submitting treatment ("Approving…", "Rejecting…", "Requesting changes…"), matching the existing pattern in `AddUserForm`/`RetryRequestButton` that these two components hadn't picked up yet. (3) Found while auditing that pattern: `admin/users/page.tsx` renders a server-side "Existing users" list below `AddUserForm`, but `AddUserForm` never called `router.refresh()` after a successful invite — the list went stale until a manual page reload, the exact "I do not want to have to reload a page to see a change" complaint the user raised, just on a page they hadn't mentioned yet. Fixed with a `router.refresh()` call after the success message is set. `npm run typecheck` and `npm run build` both pass. |
| 2026-09-18 | Copy-to-clipboard added to the sources list | User request: "Add a copy function to the sources list." Asked which scope (per-source vs. a bulk "copy all"); answer was both. `SourceList.tsx` became a client component: a small "Copy" button next to every used and considered-but-not-used source copies that one source as `"Title — URL"` (useful for pasting a citation into notes or a follow-up email), and a "Copy all" button above the list copies every *used* source the same way, one per line. Both use `navigator.clipboard.writeText` with a brief "Copied!" confirmation on the button itself; a clipboard failure (permissions, insecure context) fails silently rather than surfacing an error for what's a low-stakes convenience action. |
| 2026-09-18 | Manual section edits no longer create a new draft version per save | User feedback on the inline-editing feature shipped earlier today: "if 50 changes are made that is 50 new versions." Correct — `applyManualSectionEdit`/`handleManualEditWebhook` (mock and real) unconditionally inserted a brand-new `human_edited` draft row on every single edit, so a reviewer polishing wording over several small saves would flood `VersionHistory` with near-duplicate versions. Reworked the version boundary: the FIRST manual edit to an AI-authored draft (`system_initial`/`system_revision`/`human_requested`) still creates a new `human_edited` version, same as before — that moment (a human starting to hand-edit AI output) is worth marking. Every *subsequent* edit, as long as the active draft is already `human_edited` and no decision has been recorded against it, now updates that SAME draft row in place (`sections` + a new `updated_at`) instead of stacking another version. This is safe specifically because a `human_edited` draft never gets an evaluation or channel copy generated against it (both are intentionally skipped for this path) — nothing else in the app treats its content as immutable the way an AI-authored version's evaluation/channel-copy rows implicitly do. Each edit still gets its own `edit_section` activity-log entry regardless of whether it created a new version, so the granular "what changed and when" history is preserved even though the draft/version count isn't. Added `drafts.updated_at` (defaults to `created_at`, bumped only by an in-place edit) so `DraftViewer` can show "generated ... · last edited ..." instead of implying every edit was a fresh generation; threaded through `Draft`'s type, both `generateDraft`/`reviseDraft` implementations (mock and real), `schema.sql`, and `mock/seed.ts`. Live-project fix: `supabase/fixes/2026-09-18-draft-updated-at.sql`. Smoke-tested against a mock-mode dev server: two manual edits to the same request produced exactly one new version (v2→v3, not v3→v4), with both edits' activity notes present (`"...(now v3)"` then `"...(v3)"`) and no orphaned second draft row. `npm run typecheck` and `npm run build` both pass. |
| 2026-09-18 | Fixed "Request Changes" requiring a manual reload; surfaced *why* a request needs revision, on the detail page | User-reported: "After requesting changes, I have to reload the page before I see any UI changes." Root cause: `handleReviewActionWebhook`'s `request_changes` branch (`supabase/webhooks.ts`) was the one real-API-calling code path in the whole app still awaiting `runRevisionPipeline` synchronously on the request path, with no `maxDuration` set on `/api/webhooks/review-action` at all — unlike every other pipeline entrypoint (`content-request`, `retry-request`), which already use the `waitUntil` + `maxDuration = 300` pattern from the 2026-09-18 "Request submission no longer blocks" entry above. On Vercel this risked a 504 outright; even when it completed in time, the page had already rendered before the pipeline finished, so nothing on screen changed until a manual reload. Fixed the same way: the branch now dispatches `runRevisionPipeline` via `waitUntil` with a `.catch()` → new `recordRevisionFailure` helper (sets `failure_reason`, logs a `revision_failed` activity entry, notifies Discord), and the route gained `maxDuration = 300`. `PipelineProgress` now also polls while `status === "needs_manual_revision"` (new `stalled` prop stops polling once `failure_reason` is actually set, vs. still being in flight). This reopened a real recovery gap: `review_decisions.draft_id` has a `UNIQUE` constraint, so once "Request Changes" is recorded against a draft, `ReviewActions` is permanently locked for it ("Already requested changes") — a reviewer can't just click it again if the background revision fails. Added a dedicated recovery path instead of misleading "just resubmit" UI copy: `handleRetryRevisionWebhook` (mirrored in `mock/webhooks.ts` and `supabase/webhooks.ts`, wired through `webhookClient.ts`'s `submitRetryRevision` and a new `POST /api/webhooks/retry-revision` route) looks up the stalled request's `chosen_draft_id` and its original `request_changes` notes, clears `failure_reason`, logs a `retry_revision` activity entry, and re-runs the same revision via `waitUntil` — refusing (`not_stalled`, HTTP 409) if the request isn't actually stuck. New `RetryRevisionButton` client component surfaces this on the detail page. Second, related ask in the same message ("⚠ Needs revision — why? let the reason be shown in the details"): the detail page previously only showed *that* a request needed revision (via `StatusBadge`), not why — the reviewer's note was buried in the activity trail. Added a banner on `/requests/[id]` for `status === "needs_manual_revision"` showing the most recent `request_changes` activity entry's reviewer name and note, plus (when the automatic revision has actually failed) the failure reason and the new retry button. `ActivityAction` gained `revision_failed`/`retry_revision`; no schema migration needed for either (`activity_log.action` has no CHECK constraint, unlike `drafts.generated_by`). `npm run typecheck` and `npm run build` both pass; mock-mode's own `request_changes` path is untouched (stays synchronous — mock has no real latency to background), and its `handleRetryRevisionWebhook` was verified directly against a hand-simulated stalled state (mock mode has no real API to naturally fail, so there's no way to hit this path through the UI alone) — clears `failure_reason`, finds the right draft via `chosen_draft_id`, pulls the original notes, logs activity, and correctly refuses a second retry once the request is healthy again. Not yet exercised against a real stalled/failed revision in the live project (needs real Supabase + Claude access this sandbox doesn't have). Noted but deliberately not fixed in this pass: `select_option`'s "regenerate a never-drafted angle" sub-path (`runSelectOptionPipeline`) has the identical blocking-synchronous-call shape — same real risk, but `request.status` doesn't change during that regeneration (stays `pending_human_review` throughout, unlike `request_changes`'s natural `needs_manual_revision` transition), so fixing it needs a new UI "busy" signal, not just `waitUntil` + polling on an existing status. Flagged as a finding for the production-readiness plan requested in the same message rather than patched ad hoc. |
| 2026-09-18 | Fixed the sign-in form getting stuck on "Sending…" forever | User-reported while testing locally against real Supabase auth: "Right now I do not get the sign in link" — the form never errored, never sent, just stayed on "Sending…" indefinitely. Root cause: `middleware.ts`'s `PUBLIC_PATHS` only listed `/login` and `/auth/callback` — it never included `/api/auth/request-magic-link`, the one endpoint that has to be reachable with NO session, since it's the call the sign-in form itself makes. Every visitor attempting to sign in is by definition signed out, so the middleware's own gate (`!user && !isPublicPath` → redirect to `/login`) intercepted that POST before the route ever ran, and `fetch()` transparently followed the redirect. The client then tried to `res.json()` an HTML page (the `/login` page itself) instead of the expected `{ok: true}` body, threw a JSON parse error with nothing catching it, and left React's `status` state stuck at `"sending"` forever — a real, live bug, not specific to local dev (would have hit the same way on a real deploy). Fixed by adding `/api/auth/request-magic-link` to `PUBLIC_PATHS`. Also hardened `LoginForm.tsx` itself as belt-and-suspenders: wrapped the fetch/`res.json()` call in try/catch so ANY future failure mode (network error, a non-JSON response from some other cause) lands on a visible error state instead of an unrecoverable stuck button. `npm run typecheck` and `npm run build` both pass. |
| 2026-09-18 | Closed a second gap in the same revision-retry flow: a background job that dies silently (not just one that fails loudly) now becomes retryable too | Found while the user was testing today's request-changes fix locally: a `waitUntil`'d revision can fail two different ways, not one. The already-handled way is a caught error — `runRevisionPipeline` throws, the `.catch()` sets `failure_reason`, and `RetryRevisionButton` appears. The unhandled way: the process actually running that background work gets killed mid-flight — trivially reproducible locally by restarting `npm run dev` while a revision is in flight, and just as possible in production if a redeploy lands mid-request — and nothing ever catches anything, because nothing ran to completion; `failure_reason` stays null forever. That state is indistinguishable from "still legitimately in progress" by looking at the request row alone, so the existing `!!failure_reason` check (both in `page.tsx`'s `revisionStalled` and in `handleRetryRevisionWebhook`'s own guard, mock and real) never fires, `PipelineProgress` polls forever, and there was no button to unstick it. Fixed by adding a staleness fallback everywhere that check exists: `Date.now() - new Date(request.updated_at).getTime() > REVISION_STALE_MS` (5 minutes, matching the routes' own `maxDuration = 300` — Vercel would have killed a real run by then anyway) now also counts as stalled, in `src/app/requests/[id]/page.tsx`, `src/lib/mock/webhooks.ts`, and `src/lib/supabase/webhooks.ts`. The failure banner falls back to "This is taking longer than expected — it may have stalled." when there's no `failure_reason` to show. `handleRetryRevisionWebhook` (both modes) already resets `updated_at` on every retry attempt, so each attempt gets its own fresh 5-minute window rather than being immediately re-flagged as stale. `npm run typecheck` and `npm run build` both pass. |
| 2026-09-18 | A source Firecrawl couldn't retrieve (paywalled, blocked, dead link) now shows up in the reviewer's Sources list with a plain-language reason, instead of vanishing | User-asked: "How would this system handle the optional link in the request form where there is a paywall or the site does not allow scraping? Source [failures] from Firecrawl fail silently. [They] should appear in the sources and... state that they were not used and the reason why." Confirmed: `researchAndCurateSources` (`generate.real.ts`) already tracked every scrape failure with a typed reason (`unsupported_site` / `http_error` / `network_error`, from the 2026-09-18 "skip known domains cheaply" entry above) but only ever persisted them to `stage_logs` — an admin-only debug table nothing in the review UI reads. A candidate URL that failed to scrape simply never became a `sources` row at all, so it was invisible next to the sources curation actually chose to reject (which already show up, with their own reason, under "Considered but not used" — `SourceList.tsx`). Fixed by giving each scrape failure a matching `SourceRef` row too: new `failedSources` return value (alongside the existing `sources`/`chunks`/`scrapeFailures`), one entry per failed candidate — `selected: false`, `raw_text: null` (so it can never be cited from or mistaken for real grounding material), `title` falls back to the URL itself, and `relevance_note` is a new `describeScrapeFailure()` helper's plain-language explanation (a 401/403 names paywall/login-wall/bot-blocking as one honest possibility, not a diagnosis — Firecrawl doesn't actually tell us which; 404 names itself; a network-level failure says so; the pre-filtered known-unsupported-domain case says the site doesn't allow automated scraping). `runInitialPipeline` (`supabase/webhooks.ts`) inserts these stub rows into `sources` *before* the existing "zero usable sources → `research_failed`" check, so they show up even in the total-failure case, where a reviewer previously had no way to see what was even attempted. They land in the exact same "Considered but not used" list a curation-rejected source already uses — no new UI needed, just no longer silently dropped. Mock mode untouched (no real scraping to simulate, same reasoning as every other real-mode-only scraping fix). `npm run typecheck` and `npm run build` both pass; not yet exercised against a real paywalled/blocked URL in the live project. |
| 2026-09-18 | Added a premise check on the submit-request form — a raw idea asserting a claim against established facts (e.g. "the Earth is flat and this is why") is rejected right there, before a request is ever created | User-asked for suggestions on catching factually-false raw ideas, using that exact flat-earth example. Talked through three shapes before building: (1) a new gated pipeline stage after intake, pausing a flagged request in a new status for a reviewer to clear or reject; (2) folding it into the existing evaluation rubric, which only catches it after a full draft is already written and wouldn't catch a premise "consistent" with unreliable sources; (3) a non-blocking warning badge shown during review. User's explicit answer: "it should not get past the submit request page" — a fourth, stricter shape: reject the submission itself, synchronously, exactly like every other `validateNewRequest` failure (missing audience, invalid URL, etc.) — no request row ever created, no research or drafting cost spent, error shown inline on the form. New `checkIdeaPremise(idea_or_topic)` (real: `generate.real.ts`, via a new Claude call; mock: `generate.ts`, deterministic keyword matching against a small known-false-claims list) is called in both `handleContentRequestWebhook`s (`mock/webhooks.ts`, `supabase/webhooks.ts`) right after `validateNewRequest` passes and before anything is inserted; a flagged result returns the same `{ok: false, errors: [...]}` shape any other validation failure does, so `NewRequestForm.tsx` needed zero changes — the existing `idea_or_topic` field-error slot just renders it. Deliberately narrow in scope, and this is the part that actually matters for not breaking the tool: it only flags a claim ASSERTED as settled fact against overwhelming scientific/historical/empirical consensus (flat earth, anti-vaccine claims stated as fact, moon-landing-hoax, Holocaust denial) — never political opinions, ethical positions, or any other genuinely contested-but-not-empirically-settled viewpoint, however controversial, and never an idea that examines, reports on, or debunks a false belief rather than asserting it ("why some people believe the Earth is flat" stays unflagged; "the Earth is flat and this is why" doesn't) — the real-mode prompt and the mock's debunk-intent-word exemption both encode this line explicitly, and it's the main risk surface if this prompt is ever edited carelessly: too loose and real misinformation gets through, too tight and it starts blocking legitimate contrarian or investigative topics. Second, related ask in the same message: "use a smaller model for that check" — this is a narrow yes/no judgment on the submit-request critical path (added latency directly affects how long a submitter waits), not a generation task, so it shouldn't cost what the main pipeline model costs. Added `ANTHROPIC_FAST_MODEL` (`.env.example`, `config.ts`, defaults to a Haiku-tier model id) and gave `structuredCall` (`ai/anthropic.ts`) an optional `model` override parameter (defaults to `ANTHROPIC_MODEL` when omitted, so every existing caller is unaffected) — `checkIdeaPremise` is its first user. Smoke-tested end to end in mock mode: "The earth is flat and this is why" is rejected with no request created; "why some people still believe the earth is flat" and "debunking the flat earth myth" both pass through unflagged; an unrelated idea submits normally. `npm run typecheck` and `npm run build` both pass; the real-mode Claude call itself hasn't been exercised against a live API key in this sandbox. |
| 2026-09-18 | Fixed the magic-link sign-in redirecting straight back to `/login` instead of landing on `/board` | User-reported bug with a live example URL: `/login?next=%2F#access_token=...&refresh_token=...&type=magiclink` — the emailed link was issuing implicit-flow auth (tokens in a `#` hash fragment, which browsers never send to the server) instead of PKCE flow (a `?code=` query param). Root cause: `/api/auth/request-magic-link/route.ts`'s `signInWithOtp` call used a plain `@supabase/supabase-js` `createClient(SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY)`, and `@supabase/auth-js`'s `GoTrueClient` defaults `flowType` to `'implicit'` (confirmed by reading `node_modules/@supabase/auth-js/dist/main/GoTrueClient.js`) when nothing overrides it. `/auth/callback/route.ts` only ever looked for `?code=` and called `exchangeCodeForSession(code)` — it had no `code` to find, did nothing, and the user fell through to middleware's session check, which redirected the still-unauthenticated request to `/login`; browsers preserve a URL's hash fragment across a same-origin redirect that doesn't set its own, which is why the stale `#access_token=...` rode along onto the final `/login` URL. Fix: swapped the plain `createClient` for the already-established cookie-aware `getSupabaseRouteClient()` (`lib/supabase/routeClient.ts`, `@supabase/ssr`'s `createServerClient`, same client `/auth/callback` already uses) — confirmed via `node_modules/@supabase/ssr/dist/main/createServerClient.js` that it explicitly sets `flowType: "pkce"`. Requesting the magic link now persists a PKCE code_verifier cookie on the response, so Supabase emails a `?code=` link `/auth/callback`'s existing `exchangeCodeForSession(code)` can actually redeem into a session. No other files changed; `USE_MOCK_DATA` short-circuit and the `profiles` existence check ahead of it are untouched. `npm run typecheck` and `npm run build` both pass. Not yet re-tested against a live magic-link email in this sandbox (no network path to send one) — the user should confirm the next real sign-in attempt lands on `/board`. |
| 2026-09-18 | Reverted the custom 429 rate-limit message on `/api/auth/request-magic-link`; back to passing Supabase's `error.message` straight through | Briefly added a hand-written 429 message ("Too many sign-in emails requested... configure SMTP") after diagnosing that Supabase's default email sender — no custom SMTP configured on the project — caps outgoing auth emails at a low hourly quota, and that's why `signInWithOtp` started failing right after the PKCE fix (that fix meant the request finally reached Supabase for the first time today, instead of dying at the middleware redirect earlier). User's explicit feedback: remove the added copy, restore the previous plain `error.message` pass-through, and stop writing user-facing strings that narrate internal infrastructure detail ("Supabase's default email sender", "configure SMTP") — this is a real application, not a place for that tone. Route is back to its prior one-line `if (error) return NextResponse.json({ ok: false, message: error.message }, { status: 500 })`. The underlying rate-limit fact from the previous entry still stands as an operational note for whoever runs this project day to day; it just doesn't belong in the app's own error copy. |
| 2026-09-18 | Added password sign-in as a second option on the login page, alongside the existing magic link | User created a Supabase user with a password directly from the Supabase dashboard and asked to be able to log in with it — useful for exactly the situation that prompted it: testing sign-in without waiting on Supabase's email-sending quota. New `/api/auth/login-password` route calls `signInWithPassword` through the same cookie-aware `getSupabaseRouteClient()` used elsewhere, so the session cookie lands on the response directly — no callback round trip needed the way magic link requires, since a password sign-in returns a session immediately rather than an emailed link. Added to `middleware.ts`'s `PUBLIC_PATHS` for the same reason `request-magic-link` had to be added there: an unauthenticated POST to it would otherwise get redirected to `/login` first and never run. `LoginForm.tsx` now has two tabs, "Email link" and "Password" — the existing magic-link form is untouched under the first tab; the second is a new email+password form that POSTs to the new route and, on success, does a full `window.location.href` navigation (not `router.push`) to the destination so middleware and every server component re-read the freshly-set session cookie from scratch rather than relying on stale client-side router state. Errors surface Supabase's own `error.message` directly (e.g. "Invalid login credentials"), matching the plain-pass-through style the magic-link route was reverted to just above — no rewritten copy. `npm run typecheck` and `npm run build` both pass. |
| 2026-09-18 | Opened content visibility to every signed-in user, dropping the per-owner/reviewer RLS scoping | User: "all content that is in the database should be visible to everyone. not just the owners." The original schema.sql RLS policies (requests_select and every policy on a table hanging off requests: request_reviewers, request_attachments, sources, source_chunks, drafts, evaluations, channel_assets, publishing_queue, review_decisions, activity_log) only let a request's submitter, its assigned reviewers, or an admin see it. Replaced all eleven with `using (auth.uid() is not null)` — any authenticated profile can now see every request and everything attached to it, in both `supabase/schema.sql` (fresh installs) and a new `supabase/fixes/2026-09-18-open-content-visibility.sql` (drop-and-recreate, safe to re-run, for the live project that already ran the old schema). No app code changed: `src/lib/data.ts`'s `listRequests()`/`getRequestDetail()` already do a plain select with no ownership filter of their own — the scoping lived entirely in these RLS policies, confirmed by reading that file before touching anything. Deliberately left `stage_logs_select` untouched (still admin-only) — it's internal debug telemetry, not app content, and wasn't part of what was asked. `is_admin()`, `is_request_reviewer()`, and `is_request_submitter()` are left in place (harmless, and still referenced elsewhere) even though the select policies no longer call the latter two. The user still needs to run the new fix file against their live Supabase project via the SQL Editor — this only ships the SQL, it doesn't execute it. |
| 2026-09-18 | Rendered drafted article body and channel copy as actual formatting instead of literal Markdown, and locked channel copy to true plain text | User (referencing two checklist items): "generated articles should follow SEO best practices... channel-specific outputs should follow platform formatting rules" plus "the h1,h2,and h3... the formatted view is what should be shown (not with ** and #)". The SEO/channel-formatting rules (assets/seo-best-practices.md, assets/channel-formatting-rules.md) turned out to already be condensed into generate.real.ts's SEO_GUIDELINES/CHANNEL_FORMATTING_RULES and included in the drafting, evaluation, and channel-prep prompts — confirmed by reading the file before changing anything, so that half was already done. The actual gap was rendering: DraftViewer.tsx showed a section's title (H1) and heading (H2) as real HTML already, but its `body` and ChannelAssetsPanel.tsx's `content` were both plain `whitespace-pre-line` text, so any Markdown the model wrote inside them (### subheadings, **bold**, - lists) showed up as literal asterisks and hash marks. Two different fixes for two different destinations: (1) new dependency-free `MarkdownText.tsx` (headings #/##/###, bold, italic, bullet/numbered lists, paragraphs — a small deliberate subset, not a full Markdown library) now renders `section.body` in DraftViewer, and SEO_GUIDELINES was extended to explicitly allow Markdown (specifically H3 subheadings) inside `body` for this reason, since the article view is our own in-app rendering, never republished verbatim. (2) The opposite fix for channel copy: added a NO_MARKDOWN_RULE to every channel's prompt in CHANNEL_FORMATTING_RULES explicitly forbidding Markdown syntax, because LinkedIn/X/newsletter content is described as "ready to publish as-is" on platforms that don't render Markdown at all — rendering it prettily in our own preview would hide a real defect (literal asterisks the reviewer would actually see once published) rather than fix it, so ChannelAssetsPanel.tsx is deliberately left showing that content verbatim. `npm run typecheck` and `npm run build` both pass; MarkdownText's inline bold/italic regex was additionally smoke-tested standalone against several realistic strings. |
| 2026-09-18 | Wrote PRODUCTION-READINESS-UX-PLAN.md — a researched UI/UX audit and prioritized plan, checklist items 15/16 | Researched five categories of comparable system (Contentful's task/comment approval workflow, a CMS vendor's 5 human-in-the-loop AI content review patterns, Sprout Social's multi-channel approval process, Clearscope/Surfer/Frase's SEO score UI, and general human-AI collaboration UX patterns) via WebSearch/WebFetch, then audited every review-surface component (KanbanBoard, RequestCard, DraftViewer, ContentAngleSelector, VersionHistory, SourceList, EvaluationPanel, ChannelAssetsPanel, ReviewActions, ActivityTrail) against those patterns by reading the actual current code, not assuming. Findings: several patterns (grounded-retrieval source review, schema-constrained generation, the angle-selector's free-switch-vs-costs-a-call distinction, today's premise check as an automated pre-review gate) are already correctly implemented, sometimes ahead of what the research turned up. Real gaps found and prioritized Now/Next/Later: no personal "needs my attention" view now that today's RLS change made every request visible to everyone (flagged as the most time-sensitive item, since it's a gap this same session's earlier change created); no color-coding on EvaluationPanel's rubric scores (Clearscope/Surfer-style green/amber/red); no version diff view; no platform-accurate channel-copy previews; no reverse source-to-section lookup; no notification/digest system; no batch review (the last two explicitly deferred as lower urgency/bigger scope). This is a plan document only — nothing in it has been implemented yet. |
| 2026-09-18 | Implemented the plan's Now/Next items: "needs my attention" board filter, color-coded rubric scores, version diff badges, platform-accurate channel previews, reverse source-to-section links | User: "Let's proceed with these. The notifications are covered in discord." — confirmed discord.ts already fires at pipeline lifecycle events (notifyDiscord is called from supabase/webhooks.ts at the points a reviewer would want to know about), so the plan's "Later" notification/digest item is considered handled and wasn't built; batch review stays deferred (not requested). Five changes: (1) KanbanBoard.tsx is now a client component with an "All requests" / "Needs my attention" toggle scoped to request_reviewers — a pure client-side display filter, no new query or RLS policy, defaulting to "All" so nothing changes unless touched; board/page.tsx now also fetches getCurrentProfileId() and passes it through, tolerating null. (2) EvaluationPanel.tsx's scoreColorClass() color-codes each rubric score (emerald ≥8, amber 5-7, red <5), mirroring Clearscope/Surfer's checklist coloring. (3) DraftViewer.tsx accepts an optional previousSections prop and shows a "New"/"Changed" badge per section (matched by heading, not array position, since a revision can reorder sections) plus a collapsible "see previous version" for changed ones — scoped to section-level change detection, not word-level diffing, per the plan's explicit phase-one scope; DraftReviewPanel.tsx computes previousSections as the highest-versioned OLDER draft sharing the selected draft's option_label (angles have independent version sequences, so this isn't just "version - 1"). (4) ChannelAssetsPanel.tsx was rewritten with per-channel preview chrome — LinkedIn card framing, an X card with a live character counter against the 280 limit (matching generate.real.ts's own server-side validation), and a newsletter card that best-effort splits a leading "Subject: ..." line from the body — deliberately no fabricated engagement numbers/avatars, only structural cues that catch real problems. (5) SourceList.tsx takes an optional citingSections prop and shows "Cited in: <headings>" (or a flagged "Not cited in the current draft") under each used source — the reverse of what DraftViewer already showed; requests/[id]/page.tsx passes the ACTIVE draft's sections (not whichever version a reviewer is currently viewing, which is client-side state the server component can't see). `npm run typecheck` and `npm run build` both pass. |
| 2026-09-18 | General UI polish pass: shared component primitives for buttons, checkboxes, dropdowns, and date pickers | User-requested: "I need better ui. Buttons, clors, checkboxes, dropdowns, date picker, etc." Before this, every control in the app was a one-off `<button>`/`<input>`/`<select>` with its own copy-pasted Tailwind class string — inconsistent padding, no visible keyboard focus ring on most of them, and each button color (accent/emerald/amber/red/slate) hand-picked per call site rather than drawn from a real variant scale. Added six shared primitives under `src/components/ui/`: `Button` (variant scale — primary/secondary/accentOutline/success/danger/dangerOutline/warning/ghost — plus a `loading` state with an inline spinner), `Checkbox` (restyled native checkbox via CSS `accent-color` — keeps the browser's own accessible control, just recolored, no custom SVG box), `Select` (native `<select>` with `appearance-none` and a drawn-on chevron, so it stays a real keyboard/autofill-compatible dropdown but looks consistent across browsers), `TextInput`/`Textarea` (a shared `inputClasses` string with a real focus ring), `DateTimeField` (native `date`/`datetime-local` input, deliberately not replaced by a JS calendar widget — the native picker is already accessible and OS-consistent for free; this just adds matching border/focus chrome and a calendar icon), and `Badge` (neutral/success/warning/danger/accent status-pill tones). Extended `tailwind.config.ts` with a full `accent` color scale (50–900) so the primitives have shades to draw on beyond the single hardcoded `accent-600`/`accent-700` values used ad hoc before. Migrated every hand-rolled control across the app to these primitives: `StatusBadge`, `EvaluationPanel` and `ChannelAssetsPanel`'s status pills (`Badge`), `ReviewActions` (all three top-level actions, the publish-timing radios — recolored via `accent-accent-600` rather than a dedicated Radio primitive, since only this one call site needs radios — the scheduled-for `DateTimeField`, the notes `Textarea`, and both confirm/cancel pairs), `NewRequestForm` (tone `Select`, channel/reviewer `Checkbox`es, all text fields, the add/remove-criterion and submit buttons), `AddUserForm` (role `Checkbox`es, text fields, invite button), `RetryRequestButton`/`RetryRevisionButton` (the `dangerOutline` variant was added specifically for these), `ContentAngleSelector` (the `accentOutline` variant was added specifically for its switch/generate action), `DraftViewer`'s inline edit/save/cancel controls, and `LoginForm`'s two submit buttons. Left as custom, deliberately: `LoginForm`'s mode tab toggle and `KanbanBoard`'s "All requests"/"Needs my attention" toggle — both are a tab/segmented-control pattern, not a generic button, and already match `RequestTabs`' existing styling. `npm run typecheck` and `npm run build` both pass. |
| 2026-09-18 | Improved `structuredCall`'s failure diagnostics after a live self-evaluation JSON-parse failure with no useful detail | User hit a live Discord alert: `automatic revision failed: ... structuredCall(self-evaluation) failed after retry: SyntaxError: Unterminated string in JSON at position 245 (line 1 column 246)`. The short position (~246 characters into what should be a multi-field evaluation response) rules out simply hitting `max_tokens` (2048 for this call) and points at either a transient malformed response from the model or something in that specific draft's content confusing the evaluation prompt — but the thrown error only carried the `JSON.parse` exception, not what Claude actually returned, making it undiagnosable after the fact. `structuredCall` (`src/lib/ai/anthropic.ts`) now tracks the raw response text and `stop_reason` from the last attempt and includes a truncated (500-char) snippet of the raw response plus the stop reason in the error it throws on final failure, so the next occurrence is actually debuggable from the Discord alert / server logs alone. Deliberately did not add silent JSON-repair or an extra retry — the module's existing "never fail open" design (a malformed evaluation response should hard-fail, not be guessed at) is unchanged; this only makes the failure legible. `npm run typecheck` and `npm run build` both pass. |
| 2026-09-18 | Newsletter channel copy carved out of the blanket NO_MARKDOWN_RULE — generated with real Markdown and rendered formatted, while LinkedIn/X stay plain text | User-reported: "news letter channel copy is not formarted to use md." Confirmed against assets/channel-formatting-rules.md before changing anything: its Email Newsletter section explicitly asks for a value section that's "easy to skim with subheadings or bullets" — structure Markdown is meant for — unlike LinkedIn/X, which that same document only asks for short paragraphs/line breaks and which genuinely never render Markdown once published. Asked the user to confirm scope (newsletter only, vs. all three channels); answer was newsletter only, "follow the guides mentioned in the prd." Reworked `CHANNEL_FORMATTING_RULES.newsletter` (`generate.real.ts`): dropped the blanket `NO_MARKDOWN_RULE` for this channel in favor of a new `NEWSLETTER_MARKDOWN_RULE` that explicitly asks for `##` subheadings and `-` bullets in the main value section (no `#`/H1 — the subject line already serves that role) and an explicit `"Subject: "`-prefixed first line, matching `splitNewsletter()`'s existing parsing in `ChannelAssetsPanel.tsx`. LinkedIn and X are untouched — still under `NO_MARKDOWN_RULE`, still rendered as plain `whitespace-pre-line` text in their previews, since neither platform renders Markdown and a reviewer should see exactly what will actually get posted. `ChannelAssetsPanel.tsx`'s `NewsletterPreview` now renders `body` through `MarkdownText` (the same component `DraftViewer` already uses for the article view) instead of `whitespace-pre-line`, so `##`/`-` actually render as a subheading/list instead of showing as literal characters; `LinkedInPreview`/`XPreview` are unchanged. Also updated mock mode's `prepareChannelAssets` (`generate.ts`) to prefix each section heading with `## ` in its newsletter template, so the mock/demo path renders the same way without needing a real Claude call — mock section bodies are already plain prose with no markdown syntax, so this only adds the heading markers, nothing else changes visually for LinkedIn/X or the article view. `npm run typecheck` and `npm run build` both pass. |

## 11. Publish Queue Runner (n8n workflow) — Retired 2026-09-18

**Retired.** User-requested: n8n's automated LinkedIn/X delivery is dropped entirely, not replaced by an equivalent in Supabase or anywhere else. `publishing_queue` reverts to what §2/3 originally scoped it as: the record that satisfies the PRD's "publish or schedule for release" requirement, full stop — a row lands there on approval (`scheduled_for` set from whatever the reviewer chose), and that's the end of the automated part. No poller, no platform API calls, nothing consuming `ready_to_publish` rows. `n8n-publish-workflow.json` and `PUBLISH-WORKFLOW-SETUP.md` are kept for history (the setup doc has its own banner); this section's original body is left below for the same reason. If automated delivery is wanted again later, it doesn't have to be n8n — a Supabase Edge Function on a Cron Trigger could poll `publishing_queue` the same way, without reintroducing a separate external system, but that's a real feature to design and build (LinkedIn/X OAuth credential storage and refresh, the actual posting calls), not a drop-in replacement, and hasn't been requested.

The Discord notifications this workflow used to send (publish failures, stale posts, repeated-failure alerts) don't apply anymore — there's no automated publishing left to alert on. What replaced them is different in kind: `src/lib/discord.ts`, called directly from the Next.js pipeline (`src/lib/supabase/webhooks.ts`), notifies on the *generation* side instead — a draft is ready for review, or the pipeline itself failed. See the 2026-09-18 Decisions Log entry.

<details>
<summary>Original section, kept for history</summary>

Section 2/3 described the publishing queue as the end of scope. That's now extended: a dedicated n8n workflow (`n8n-publish-workflow.json`, setup notes in `PUBLISH-WORKFLOW-SETUP.md`) polls `publishing_queue` every 5 minutes and publishes due items automatically.

**Flow:** Schedule Trigger (5 min) → query `publishing_queue` for rows with `status = 'ready_to_publish'` and `channel IN ('linkedin','x')` where `scheduled_for` is null (publish ASAP) or already due → process one row at a time → route by channel → LinkedIn node / X node → update the row to `published` or `failed` (with the error message) and log the attempt to `stage_logs`.

**Scheduling rule:** `scheduled_for` must be either null (publish as soon as approved/claimed) or a timestamp strictly in the future at the moment it's set — this is validated at the point of scheduling (intake or review UI), not left for the runner to discover a stale date later. Combined with the stale-schedule guard already in the runner (§11 above, hardening pass), this means a scheduled time can never end up in the past by the time it's actually due, whether that's from user error or system downtime.

**Scope note:** Medium and the email newsletter are intentionally not wired into this workflow yet — the queue still stores those rows with `status = 'ready_to_publish'`, but nothing currently picks them up. That's a deliberate scope cut, not an oversight; revisit if/when those channels need automated publishing too.

**Failure handling:** each platform node runs with "continue on error" so one failed post doesn't block the rest of the batch — it's marked `failed` with the API's error message and logged, and stays visible for a human to retry or triage rather than silently disappearing.

**Discord notifications (2026-09-16):** the runner now posts to a Discord webhook whenever something needs a human's attention: a publish failure (per row, with the actual error), a validation failure (content rejected before it ever reached the platform API), a batch of newly-flagged stale posts (one summary message, not one per row), and a repeated-failure escalation (3+ failures on one channel within an hour — this is what actually surfaces a silently-expired credential, closing the gap flagged in the earlier hardening pass). Successful publishes are deliberately **not** pinged by default — see `PUBLISH-WORKFLOW-SETUP.md` §4 for the reasoning (Discord stays an alert channel, not an activity firehose) and how to add success notifications if wanted.

**Hardening pass (2026-09-15):** a review against the PRD's edge cases (full writeup in `EDGE-CASES-AND-GUARDRAILS.md`) found and fixed a real double-publish race condition — the runner now atomically claims rows (`FOR UPDATE SKIP LOCKED`) instead of a plain `SELECT`, so two overlapping executions can never publish the same row twice. It also added a stale-schedule guard (anything more than 24h overdue is pulled into `stale_needs_review` instead of being auto-blasted after downtime) and a validation gate that checks content is non-empty and within each channel's limits *before* any platform API call, marking violations `failed` with a specific reason instead of letting the API reject them opaquely. Credential-expiry alerting and rate-limit-aware retry were identified but not yet built — see the review doc's "known gaps."

</details>

## 12. Full Edge Case & Guardrail Review

See `EDGE-CASES-AND-GUARDRAILS.md` for the complete stage-by-stage review: required inputs and failure behavior for every stage from intake through publishing, cross-cutting hardening (treating scraped/model text as untrusted data, schema-validating every LLM structured output, never failing open, cost/iteration caps, dedup), and suggested additions to the PRD's testing evidence table.

A second, more product-focused round of questions (rubric extensibility, draft versioning, supporting-material uploads, multi-reviewer review, reject vs. request-changes, scheduling, cross-request failure isolation, context window handling, timing estimates) is answered in the sections below and logged in full in `EDGE-CASES-ADDENDUM.md`.

## 13. Custom Rubric Criteria (Extensibility)

The PRD's rubric (`assets/content-evaluation-rubric.md`) defines 9 default criteria — Topic Relevance, Source Grounding, Factual Consistency, Audience Fit, Tone, SEO Fit, Channel Fit, Clarity, Completeness. Those always apply, and the default rubric file itself is never edited — it stays the PRD's source of truth.

On top of that, the design now supports **custom criteria that are additive, not a replacement**, stored in `rubric_criteria` (§6) and scoped to a channel and/or content type (e.g. a criterion that only applies to LinkedIn posts, or only to anything tagged "client-facing"). At evaluation time, the evaluation prompt includes the default 9 plus whatever active custom criteria apply to that specific request's channel/content type, and the evaluation's structured output extends to score each one by name — so a custom criterion is scored exactly like a default one, just clearly labeled `scope: custom` in the output so it's obvious in review which criteria are PRD-standard and which were added.

**Suggested custom criteria worth having available out of the box** (all optional/additive, none replace the default 9):
- **Hook Strength** — does the opening line earn a read, especially on LinkedIn (truncates after ~3 lines) and X (no second chance)?
- **Actionability** — does the reader walk away with something concrete to do, not just information?
- **Differentiation** — does it say something not already said identically by the top competing articles found during research, or is it a generic rehash?
- **Data/Stat Accuracy** — for any numeric claim, does it match the source exactly (no rounding drift, no misattributed figure)? Especially relevant when supporting material is a spreadsheet.
- **Citation/Link Validity** — do included links actually resolve and match the claim they're attached to?
- **Brand Safety / Compliance** — flags competitor bashing or unverified legal/medical/financial claims for a human's attention rather than blocking outright.
- **CTA Effectiveness** — is the call-to-action specific and channel-appropriate, not a generic "learn more"?
- **Accessibility & Readability** — reading level appropriate for the stated audience, jargon explained, alt text present if an image is included.

**Open governance question:** who gets to create/edit custom criteria — any content manager, or a content lead only? Flagged in `BUSINESS-QUESTIONS.md` rather than decided here.

## 14. Supporting Material (Text & File Uploads)

Beyond a source URL, a request can include supporting material as pasted text or an uploaded file. Accepted formats: **`.txt`**, **`.pdf`**, a **spreadsheet** (`.xlsx`/`.csv` — for numeric or tabular source data), and **`.docx`**. Anything else is rejected at upload with a clear message listing the accepted types — never silently ignored or passed through.

**Corrupt files:** every upload is parse-tested immediately (can it actually be opened/read as its claimed type?). A file that fails is marked `corrupt` in `request_attachments` and the submitter is asked to re-upload — a corrupt file is never handed to research/drafting as if it were readable.

**Edit/removal:** attachments can be edited or removed by the submitter any time before the request has actually entered the Research & Retrieval stage. Once research has already consumed a file (chunked and embedded it), removing the attachment record stops it from being used in any *future* regeneration but doesn't retroactively strip already-embedded chunks from a draft that already used them — that would silently change grounding underneath a draft someone may already be reviewing. Every add/edit/removal is written to `activity_log`.

**Large files & the context window:** a large PDF or spreadsheet is chunked and embedded exactly like a scraped web source (§ Retrieval), not stuffed into a single prompt whole — see §16.

## 15. Failure Handling & Isolation Across Requests

The PRD is explicit that a failure in one stage shouldn't take down the whole system, and that's a structural property of the design, not just an error-handling afterthought: **every request is processed as an independent unit of work keyed by its own `request_id`**, with its own status and its own row in every table. A failure at any stage — research finding nothing, a malformed LLM response, a publish-destination error — updates *only that request's* status (e.g. `research_failed`) and writes to `stage_logs`; it never throws an exception that could stop the orchestrator from picking up other, unrelated requests. Concretely: the n8n orchestration processes requests as separate items/executions rather than one shared in-memory loop, so one request stuck retrying research doesn't block another request that's ready to move to drafting.

This is the same principle already built into the publish runner (§11): one row failing to post doesn't block the rest of the batch. It just applies end-to-end, not only at the publishing stage.

## 16. AI Context Window Management

Nothing in the pipeline feeds raw, unbounded source text into a single LLM call:

- **Retrieval is top-K, not top-everything.** Scraped/uploaded source text is chunked and embedded (pgvector) once, and any given prompt (planning, drafting, evaluation) only pulls in the chunks relevant to what that call needs — a 50-page source contributes a handful of relevant excerpts to a given section, not its full text.
- **Each stage's prompt only carries what that stage needs.** Evaluation, for example, doesn't need the entire source corpus resent — it needs the draft plus the specific excerpts the draft cited.
- **Structured outputs keep responses compact** — a JSON object with defined fields is naturally bounded, unlike free-form prose.
- **A token budget is set per call, well under the model's actual context limit**, leaving headroom for the system prompt, retrieved context, and the response itself; if a single request would need more than that budget (e.g. an enormous uploaded document), it gets summarized/re-chunked further rather than truncated silently.

## 17. Estimated Timings

Rough, pre-build estimates — to be replaced with real numbers once Firecrawl/search/embedding are actually wired up and benchmarked:

- **Single URL scrape (Firecrawl):** ~2–8 seconds per URL.
- **Search + scrape for a raw idea (3–5 sources):** ~10–30 seconds if done sequentially, ~5–15 seconds if parallelized.
- **Chunking + embedding into Supabase:** a few seconds for a typical article-length source; longer for a large uploaded document.
- **Research & Retrieval stage, end to end:** roughly **15–45 seconds** for a typical request, up to a minute or two in a worst case (slow sites, retries, a large uploaded file).
- This is separate from drafting/evaluation time (the LLM calls), which will add its own latency on top — a full first-draft turnaround (research through a passing evaluation) is likely a small number of minutes rather than seconds, before any human review time.

## 18. Notification Service (Discord)

A cross-cutting service, not tied to one stage: whenever something in the pipeline needs a human's attention, it posts to a Discord channel via an incoming webhook (no bot, no OAuth — see `PUBLISH-WORKFLOW-SETUP.md` for setup). The design intent is that Discord is an **alert channel**, not a log stream — it only speaks up for things that are actionable or worth a human's awareness, mirroring the "gate, don't guess" principle the rest of this doc leans on: quiet by default, loud when something needs a look.

**Built (in `n8n-publish-workflow.json`):**
- Publish failure, per row, with the actual platform error (LinkedIn/X).
- Validation failure — content rejected before it reached a platform API, with the specific reason.
- Stale scheduled posts flagged — one summary message per batch, not one per row.
- Repeated-failure escalation — 3+ failures on one channel within the last hour, the signal that something structural (an expired credential, a persistent rate limit) is wrong rather than one-off bad luck.

**Designed, not yet built** (these stages aren't implemented as actual workflow code yet — §2's earlier stages are still design-only, so there's nothing to wire a notification into yet, but this is where it would hook in once they are):
- `research_failed` — a request found zero usable sources.
- Evaluation escalation — the rubric evaluator's output failed to parse twice in a row (§ "never fail open," `EDGE-CASES-AND-GUARDRAILS.md`).
- Revision cap hit — a draft used all its revision rounds without passing (`needs_manual_revision`).
- A draft is ready for human review (lower priority — arguably a digest rather than a per-item ping, to avoid the same "firehose" problem success notifications would cause on the publish side).
- Corrupt or rejected supporting-material upload.
- Human review actions (approve/reject/request changes) — optional, mainly useful if a team wants visibility into review activity beyond `activity_log` itself.

Each of these, once its stage exists as real workflow code, is just another write to `stage_logs`/`activity_log` with a Discord node attached alongside it — the pattern is already established by what's built.

## 19. Intake, Research & Retrieval, Source Curation — Detailed Spec

**Superseded 2026-09-16.** This section originally described these three stages as n8n's job, spec'd in `INTAKE-AND-RESEARCH-SPEC.md`. Both the framing and the document are superseded — intake, research, curation, and everything through channel prep now live in the Next.js pipeline described in `CONTENT-PIPELINE-SPEC.md`, which is the current spec and is now built as real code (not just designed) in `frontend/src/lib/pipeline/`. `INTAKE-AND-RESEARCH-SPEC.md` is kept for history with a banner pointing here.

## 20. Standalone LLM Service

**Superseded 2026-09-16 — see the Decisions Log.** This section originally called for a standalone Python/FastAPI service, called over HTTP from n8n, to own every Claude API call. That's reversed: planning, drafting, evaluation, and revision are now Claude API calls made directly from the same Next.js server code as the rest of the pipeline — no separate service, no HTTP hop, no second deployment target. The "never fail open" evaluation contract and context-budget reasoning from the original spec still apply exactly as written; only *where the code runs* changed. Current spec: `CONTENT-PIPELINE-SPEC.md`. `LLM-SERVICE-SPEC.md` is kept for history with a banner pointing here.

## 21. Custom Front-End (Intake + Human Review)

Replaces the n8n-Form-Trigger intake plan from earlier in this section — full reasoning, architecture, auth/roles, both flows (submit a request, review a draft), and the webhook-shaped contracts it uses internally (`content-request`, `review-action`, `manage-attachment`) are in `FRONTEND-SPEC.md`. The short version: one Next.js app serves both the content-manager-facing submission form and the reviewer-facing dashboard, reading Supabase directly for anything that's just display and routing state-changing actions through its own trusted server layer (never the browser directly) — so client-side validation can be as strict and pleasant as it likes (a date picker that structurally can't select the past, instant file-type rejection) without ever being the thing the system actually trusts; the server layer still re-validates everything on receipt.

**Revised 2026-09-16:** those three contracts no longer forward to n8n. `content-request` now runs the full pipeline (§22) directly; `review-action` records the decision and, on approval, promotes this request's channel assets to `ready_to_publish`, or, on request-changes, triggers an immediate revision pass — all in the same process. n8n is no longer in the write path for intake or review at all; its only remaining job is the scheduled publish runner (§11).

**Built (2026-09-16):** a runnable Next.js scaffold exists in `frontend/` — Card/Kanban board (`/board`), the submit-request form (`/requests/new`), and the full draft-review screen (`/requests/[id]`: version history, draft viewer with per-section citation flags, evaluation panel including custom rubric criteria, channel-adapted copy for every priority channel, source list, activity trail, and approve/request-changes/reject actions). It runs entirely against seeded mock data (`NEXT_PUBLIC_USE_MOCK_DATA=true`, the default) so it's clickable end-to-end with no live Supabase or n8n — the mock re-implements the behaviors that matter, not just the screens: the `review_decisions` race guard (a second reviewer's decision on an already-decided draft is refused with a 409 and shown as a conflict banner), a `stale_draft` guard (a decision aimed at a superseded draft is also refused with a 409, not silently applied), and, since §22, the full pipeline itself running synchronously on submission. `npm install && npm run build && npm run typecheck` all pass; see `frontend/README.md` for how to run it and "Going from mock to real" for the cutover path (every read goes through `src/lib/data.ts`, every write through `src/lib/webhookClient.ts`, both gated on `USE_MOCK_DATA`, so no page or component needs to change when real Supabase/Claude/Firecrawl/Tavily/Voyage are wired in).

## 22. Content Generation Pipeline (Research → Channel Prep)

Supersedes §19 and §20. Full spec — request lifecycle, each stage's exact input/output shape, the mock-vs-real split, and the reasoning for moving this into Next.js instead of n8n + a standalone service — is in `CONTENT-PIPELINE-SPEC.md`. Built (2026-09-16) as real, runnable code in `frontend/src/lib/pipeline/` (`generate.ts` for the content-generation primitives, `run.ts` for orchestration), wired into the intake and review-action handlers in `frontend/src/lib/mock/webhooks.ts`. Verified live: submitting a new request runs research → curation → planning → drafting → evaluation → (when the first pass leaves a section uncited) one automatic revision round → channel prep for every priority channel, landing at `pending_human_review` with a full draft, evaluation history, and per-channel copy — including newsletter — already there for a reviewer to open, with no n8n or external API involved in mock mode.
